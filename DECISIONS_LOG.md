# DECISIONS_LOG.md

Every agreed decision goes here as a short bullet. This file is the source of truth — paste it into any new Claude Code sprint or Claude chat before starting work. Nothing here changes without discussing it first.

## Product
- Kitchen compliance enforcement app (UK), not a checklist app
- Tasks require proof (tick/data/photo/note) — can't be faked or skipped
- Critical/high-risk failures require a corrective action before continuing

## Process
- Restarting the build was rejected — reconcile the existing prototype, don't rewrite from scratch
- Every sprint must be committed to git as a save point
- Claude keeps answers short and simple for the user
- Claude pushes back on ideas that don't fit the ICP or app, and suggests improvements

## Standing process rule (effective immediately, all future sprints)
If Claude believes something in an approved plan should change during a build, it must STOP and ask before implementing it — not implement the change and report it afterward, even if the change seems more correct or more consistent with locked architecture. Flag it, wait for a decision, then proceed.
This applies retroactively to nothing already built — Sprint 014's versioning correction (built as an on-the-spot fix to match ARCHITECTURE_LOCK's Versioning Rule, then disclosed) stands as-is and was a reasonable call under the rules in force at the time. This rule governs how deviations from an approved plan are handled from Sprint 015 onward.

## Data storage (Sprint 001)
- Use drift for local persistence (relational, matches ARCHITECTURE_LOCK's entity list, type-safe, works with Riverpod streams)
- Keep the flat TaskLogEntry shape for now, renamed/moved to shared/models/ as TaskSubmission — no TaskTemplate/TaskInstance split yet
- Add a nullable photoPath column now to avoid a later migration
- No sync-state fields yet, no site/area/user linkage yet (Phase 2 auth/roles isn't real yet)
- Repository is append-only: submit / getAll / getByStaff / getByDateRange / watchAll — no update or delete
- Repository interface and implementation both live in shared/repositories/

## Auth and roles (Sprint 003)
- Real auth is local-only: PIN per staff member, checked against the drift DB. No server/cloud identity yet.
- Only two role tiers implemented now: staff and manager. Director tier deferred — no dashboard exists yet for it to land on.
- Login UX: tap your name/job-title tile from a list, then enter a 4-digit PIN (not a bare PIN pad with no name list).
- PINs are hashed (salted) before storage, even though everything is local-only.
- No staff-management screen yet — this sprint seeds a fixed demo staff list (reusing the existing mock names) with placeholder PINs. Real staff CRUD is deferred to a later Settings-feature sprint.
- Sprint 003 builds the auth foundation + real login screen only (schema, model, repository, providers, login UI). Wiring the logged-in session into TaskController (real completedBy) and role-based screen gating is Sprint 004, following the same build-then-wire pattern as Sprints 001/002.

## Session lifetime (Sprint 004)
- Not a persistent login. The session auto-resets to the login screen once all tasks for that session are complete (device hand-off point for the next staff member), rather than requiring manual logout.

## Full product vision (confirmed, expands original scope)
- Three role tiers: top (C-suite, company-wide + stakeholder data), mid (managers/supervisors - assign tasks, manage staff, notified on triggers), base (workers - task execution)
- Task library: grouped by operation segment/role, site-editable, with trigger events (outside min/max), fix instructions, if/then logic
- Each task has a configurable method (e.g. thermometer+photo, filter type choices) and custom fields - not just pass/fail/temperature
- Multiple named instances per equipment type (Fridge 1, Fridge 2, etc.), set up by manager/top tier per venue
- Venue setup wizard: equipment, operational points, staff + roles - guided, intuitive
- Company/branch branding controlled by top tier (colors, logo, contact info)
- Onboarding: manager selects staff, assigns tasks + frequency via tick-box library, can add custom tasks
- End-of-session summary: pass/fail + triggers, sendable to a selected manager
- Trigger notifications: configurable by top/mid tier (push and/or email), top tier can override mid-tier settings
- Shift handover: carry-over notes between shifts
- Multilingual: each staff member selects their own language
- Audit trail: append-only, versioned - nothing overwritten; manager changes to task setup (limits, frequency) create a new version, old version stays visible
- Legal limit checking: task limits checked against a legal-minimum reference table (e.g. fridge/freezer/hot-hold temps); attempts to set outside legal allowance trigger a warning
- Offline: each device queues entries locally with timestamp, syncs when reconnected - no merging/overwrite, all entries additive; manager dashboard shows a "not yet synced" indicator
- Units: support both metric/imperial and Celsius/Fahrenheit
- Inspection data export: management/top tier only

## Sprint sequence (post Sprint 005)
006 three-tier roles | 007 task library data model (method/limits/triggers/if-then/custom fields) | 008 venue setup wizard | 009 staff onboarding + task assignment | 010 richer task input UI | 011 shift handover + summary/report | 012 notifications | 013 audit trail versioning | 014 offline queue + sync indicator | 015 branding | 016 multilingual | 017 inspection export | 018 visual/UX redesign

## Three-tier roles (Sprint 006)
- Confirmed title → tier mapping: Base = Kitchen Porter, Prep Cook/Commis, Line Cook/Chef de Partie, Sous Chef/Shift Lead. Mid = Head Chef/Kitchen Manager, General Manager. Top = Area Manager, Operations Manager, Group Executive Chef, Director/MD.
- Area Manager placed at Top, not Mid — multi-site scope fits PROJECT_BIBLE's "top tier" definition (company-wide oversight) better than day-to-day single-venue management.
- Top tier gets a bare placeholder screen this sprint (logout only, no dashboard content) rather than sharing the manager log with Mid — cheap now, avoids having to unwind a "top==mid for now" shortcut later when Sprint 017 builds the real dashboard.
- Manager-view icon on the task screen is visible to both Mid and Top (both are oversight roles), not Mid only.
- RoleTier is a rename (staff/manager → top/mid/base), not additive — existing seeded users and any real on-disk data get migrated via a real schema migration (schemaVersion bump + remap step), consistent with how every prior schema change in this project has been handled, not a wipe-and-reseed shortcut.

## Task library data model (Sprint 007)
- Legal-limit violations are warning-only — a manager can still save a template whose limits fall outside the legal reference range, matching PROJECT_BIBLE's literal wording ("trigger a warning"), not a hard block.
- TaskTemplate keeps an optional EquipmentType link (the category, e.g. "Fridge") — not pushed entirely down to TaskSchedule — so a future tick-box library (Sprint 009) can filter "which templates apply to Fridge-type equipment."
- Custom fields are stored as a JSON blob column (customFieldsJson) rather than a separate child table, since the full range of field types the real 100+ tasks will need isn't known yet.
- If/then logic for v1 is minimal structured fields (isCritical, requiresCorrectiveActionOnFail) rather than a generic rule engine — deferred until a concrete task actually needs more.
- EquipmentType and LegalLimitReference get a handful of seeded example rows (Fridge/Freezer/Hot-hold unit; fridge/freezer/hot-hold legal temps) so the legal-limit check is testable, mirroring how Users got seeded in Sprints 003/006. One example TaskTemplate ("Check Fridge Temperature") is also seeded for the same reason — this is illustrative test data, not the start of loading the real 100+ task library.

## Venue setup wizard (Sprint 008)
- No Site/Organisation/Brand this sprint — the app stays implicitly single-venue; Area and Equipment don't get a siteId yet since Site doesn't exist.
- Equipment.areaId is required in the wizard UI but nullable at the schema level, matching how other optional-but-usually-set FKs have been handled.
- Staff creation lives on the existing UserRepository (new createStaffMember method) rather than a new repository.
- Entry-point icons on ManagerScreen and TopScreen are included in this sprint, not deferred — a wizard nobody can reach isn't useful, and each addition is a single icon button, not a redesign.
- Light suggestion chips (common area names) and auto-numbered equipment names (e.g. "Fridge 3") are in scope for v1 — kept intentionally simple, no recommendation engine.

## Staff onboarding + task assignment (Sprint 009)
- TaskSchedule uses a simple `active` boolean toggle for unassign/reassign, not full append-only versioning like TaskTemplate — full reassignment history is deferred until ReassignmentLog itself gets built.
- Frequency is a small fixed enum (daily/weekly/per-shift/custom) plus a free-text detail field for the custom case — granular scheduling (specific times, shift-phase-aware) is deferred to Sprint 010/011.
- Custom tasks default to `segment: 'custom'`, reusing the existing TaskTemplate field rather than adding a dedicated isCustom flag.
- The assignment screen has its own entry-point icon on ManagerScreen/TopScreen, separate from Sprint 008's venue-setup icon, keeping the two features cleanly split.
- This sprint assigns tasks to named individuals only, not broadcast to a whole role tier — role-wide assignment is a different, deferred model.

## Richer task input UI (Sprint 010)
- TaskSubmission gains traceability columns (taskScheduleId, taskTemplateGroupId, equipmentInstanceId, customFieldValuesJson) — needed for real audit traceability, not optional.
- For tasks with a numeric range, PASS/FAIL is now auto-derived from whether the reading falls inside [minLimit, maxLimit], replacing the old manual PASS/FAIL button press for those tasks specifically. Non-measurable tasks (choice-only, notes-only) keep the manual PASS/FAIL buttons.
- Units: per-user preference (temperature only — Celsius/Fahrenheit), template stays canonical, conversion happens only at the display/input edges. The schema field and conversion logic are built now; the self-service toggle UI itself is deferred to Sprint 016 — everyone is defaulted to the template's native unit until then.
- Legal-limit reference numbers stay a manager-configuration-time concern, not shown to staff during data entry — only the template's own min/max and fixInstructions are surfaced.
- The carousel shows all of a staff member's active schedules every session (matching today's fixed-list simplicity) — due/overdue tracking (TaskInstance) is a separate, later concern.

## Equipment type library expansion (Sprint 011)
- Found "Full check list.docx" (in Documents\AA Tango Sierra) — the real source checklist — was never actually incorporated into PROJECT_BIBLE.md despite being referenced as if it were; extracted it directly for this sprint rather than guessing at equipment coverage.
- Expanded EquipmentType seed list to 18 realistic commercial-kitchen types, derived from the checklist's equipment-related sections (refrigeration, fryer, cooking line, wash-up) plus the user's explicit examples (walk-ins, ice machines, prep stations). Deliberately excluded utility-style items (gas/water supply) and consumable/tool items (knives, chopping boards) — they don't fit the "named instance" model (Fridge 1/Fridge 2) the way durable appliances do.
- Did not also expand LegalLimitReference or add new TaskTemplates for the new types — out of scope for this fix, which is specifically the equipment type library plus the custom-type escape hatch. Loading the real 100+ task library remains a separate, later effort.
- Equipment type seeding is now always-ensured on every app open (idempotent, checked by name) rather than gated on "table empty" — an existing install with only the original 3 types gets backfilled with the rest. No schema version bump needed since this is pure reference data, not a structural change.
- The "Something else..." custom equipment type option follows the identical pattern to Sprint 009's custom task capability: inline creation, immediately usable, no separate screen.

## PROJECT_BIBLE checklist incorporation (docs-only pass)
- Incorporated "Full check list.docx"'s structure (19 operational segments, task method vocabulary, priority levels, frequency vocabulary) into PROJECT_BIBLE.md as a new "Task Library Source Content" section — the taxonomy, not the ~100+ individual task rows, which stay in the source document until a future sprint actually loads them.
- Confirmed the checklist file is unchanged since Sprint 011's extraction (byte-identical), so no new equipment-type gaps found this pass.

## Shift handover + end-of-session summary (Sprint 013)
- TaskSubmission gains completedByUserId — needed for reliable session-boundary queries (session = submissions by this user since the carousel loaded), replacing reliance on the formatted completedBy display string.
- "Trigger" and "FAIL" are treated as the same signal for this sprint's summary — no separate trigger-fired flag exists yet.
- Handover notes are per-venue (one running note, shown to everyone at session start) — matches the app's continued single-venue scope; per-area is a future refinement once tasks/staff are area-aware.
- "Send to manager" is a real lightweight in-app inbox (new SessionSummaries table with sentTo/acknowledged), not a no-op — gives staff a distinct, explicit "flag this for a specific manager" action beyond the general submission log a manager can already browse.
- No new Shift/session entity — session boundary is a client-side timestamp captured when the carousel loads; Open/Mid/Close shift-phase tracking remains unbuilt (PROJECT_BIBLE concept only, not yet in the schema).
- Kept as one sprint (~13 files) rather than split, since both features share the same UI moment (start/end of session).

## Notification rules (Sprint 014)
- `NotificationRule` follows the append-only/versioned pattern (`ruleGroupId`/`versionNumber`/`previousVersionId`), matching `TaskTemplate`, not the simple `active` toggle `TaskSchedule` uses — required by ARCHITECTURE_LOCK's Versioning Rule, which explicitly lists `NotificationRule` as a configurable setup entity. "Deactivating" a rule means saving a new version with `active: false`, never mutating the existing row.
- Trigger scope: `taskTemplateGroupId` nullable — null means "any task fail," non-null scopes to one task template (soft reference, same approach as `TaskSchedule.taskTemplateGroupId`). No segment- or equipment-type-level scoping yet.
- A rule must target either a role tier or a specific user (validated in the repository, throws if neither is set). Who can receive a rule isn't restricted to mid/top — the schema allows any target; who can *set* a rule is enforced by which screens expose the entry point (both ManagerScreen and TopScreen, per PROJECT_BIBLE's "configurable by top and/or mid tier").
- Split the original 5-point proposal into two sprints: this sprint builds the schema, repository, and rule-management UI only — unwired. Firing a notification on FAIL and an in-app notifications inbox are deferred to a follow-up sprint, matching this project's established build-then-wire pattern (001→002, 003→004, 007→010).
- Channel (push/email) is captured as configuration intent only on the rule — no real dispatch exists (no backend, Phase 6 territory). The rule-creation form shows an explicit note that delivery isn't connected yet, rather than hiding the toggles.
- Top-tier override of mid-tier rules (per PROJECT_BIBLE) is intended to resolve at query time — the higher tier's active rule wins for a matching trigger scope, nothing gets marked "overridden." `setByTier` is stored to support this, but the actual precedence resolution isn't implemented until the firing-logic follow-up sprint.
- A widget-level automated test for the new screen was attempted but dropped after an unresolved `pumpAndSettle()` hang (compounded by a mid-session disk-space exhaustion that also caused unrelated test/build failures). Verified instead via a repository-level versioning test (passed) plus a real Windows debug run against the existing dev database confirming the schemaVersion 8→9 migration executes cleanly. Interactive click-through of the new screen was not independently exercised this sprint.

## Multi-site foundation (inserted before notifications-firing/branding)
- Decision: build the Organisation -> Site schema foundation NOW, not deferred to the end. Reasoning: branding is explicitly per-branch (already next in sequence), and retrofitting a "which site" link onto everything already built (Users, Areas, Equipment, TaskSchedules, NotificationRules) gets more expensive the longer it's deferred.
- Scope split: build the SCHEMA foundation now (Organisation, Site entities; existing entities gain siteId). Defer the full regional-tier permissions/UI (e.g. "100 branches, regional managers between top and mid") until there's real need - that layer is additive on top of a properly-scoped foundation, not urgent today.
- This pauses the in-progress "wire notifications to fire" follow-up sprint until the foundation exists, so that sprint doesn't have to be redone site-aware later.

## Multi-site foundation schema (Sprint 015a)
- No `Brand` entity this sprint, despite ARCHITECTURE_LOCK listing Organisation/Brand/Site as three separate entities — `Site.organisationId` points straight at Organisation for now. Brand is deferred to Sprint 015's branding work; adding a `brandId` FK later is cheap and non-disruptive.
- `TaskTemplates`, `EquipmentTypes`, `LegalLimitReferences` stay organisation-wide/unscoped (no `siteId`/`organisationId`) — accepted as a known gap against PROJECT_BIBLE's "site-editable" task library wording. Real per-site task library customization is materially bigger work (independent copies vs. site-level override versions via the existing `previousVersionId` chain) and is deferred to a future sprint, not conflated with this schema foundation.
- Default Organisation/Site are auto-seeded (no setup step) with placeholder names "My Organisation" / "Main Site" — matches this project's established auto-seed-don't-block pattern. Renaming them is a later sprint's UI.
- Site-scoped tables get a `siteId` column that's nullable at the SQL level (a SQLite/drift constraint — `ALTER TABLE ADD COLUMN` can't retroactively enforce NOT NULL against existing rows) but is treated as required by application code, validated at the repository layer — same approach already used for `NotificationRule`'s "must have a target" check.
- `NotificationRules.siteId` is nullable *by design*, not just migration necessity — null means "applies org-wide across every site," same pattern as that table's existing `taskTemplateGroupId`.
- Sprint split confirmed: 015a (this sprint) builds Organisation/Site schema, models, repositories, providers, and default-site seeding only — no existing table gets a `siteId` yet. Follow-up sprints add `siteId` to the 7 site-scoped tables in clusters: (Users, Areas, EquipmentInstances), then (TaskSchedules, TaskSubmissions), then (ShiftHandoverNotes, SessionSummaries, NotificationRules) — each wiring its own UI call sites to a new `currentSiteProvider`.

## Multi-site foundation: Users/Areas/EquipmentInstances (Sprint 015b)
- `beforeOpen` reordered: `_ensureDefaultOrganisationAndSite()` now runs first (previously last), since user seeding needs a real site id to seed into. A real change to Sprint 015a's code, not purely additive — flagged and confirmed before building, not decided unilaterally.
- Domain model `siteId` is non-nullable (`int`, not `int?`) on `User`, `Area`, `Equipment`, even though the underlying DB column is nullable (SQLite/drift migration constraint) — repositories assert non-null when mapping rows, since `beforeOpen`'s backfill guarantees every row has a real value by the time application code reads it.
- `getAll()` reads are not filtered by site — no `siteId` parameter added to any read method. Only one site exists and no site-switcher UI exists yet; a filter parameter would be unused code built for a hypothetical.
- `staff_assignment_screen.dart` needed no changes — it only reads Users/EquipmentInstances (for pickers) and creates `TaskSchedules`/`TaskTemplates`, neither in this cluster's scope. Confirmed before building, not assumed.
- Only one real call site existed for all three tables' create methods: `venue_setup_wizard_screen.dart`'s `_addArea`/`_addEquipment`/`_addStaff`. All three now resolve `currentSiteProvider` and pass its id through.

## Multi-site foundation: TaskSchedules/TaskSubmissions (Sprint 015c)
- Neither real call site needed `currentSiteProvider` — both already held a site-bearing `User` in hand. `TaskSchedules.assign()` (called once, from `staff_assignment_screen.dart`) now derives `siteId` from the assigned staff member's own `siteId`; `TaskSubmissions.submit()` (via `TaskController.logTaskSubmission`) derives it from `_currentUser.siteId`, a field the controller already holds. Confirmed by reading the actual call sites before building, not assumed from the 015a plan's shorthand.
- `_backfillSiteIds()` extended to cover these two tables rather than adding a second near-duplicate function — one idempotent routine, growing per cluster.
- Backfill of pre-existing rows still uses the default site id (same as 015b) — the "derive from the acting user" approach only applies to new creates going forward; historical rows have no acting-user context to derive from.

## Multi-site foundation: ShiftHandoverNotes/SessionSummaries/NotificationRules (Sprint 015d) — closes the 015a/b/c/d split
- `ShiftHandoverNotes`/`SessionSummaries` got the standard treatment (required `siteId`, backfilled to the default site, non-nullable domain model) — both describe something that happened at one specific venue.
- `NotificationRules.siteId` is nullable in both the DB column and the domain model, and — unlike every other table in this effort — pre-existing rows were deliberately **excluded** from `_backfillSiteIds()` rather than backfilled to the default site. Sprint 014-era rules meant "applies everywhere" before Site existed; backfilling them to one specific site would have silently narrowed their scope. Leaving them `NULL` preserves that original org-wide meaning.
- New rules created via `notification_rules_screen.dart`'s form default to the creator's own site (`setBy.siteId`), not org-wide — no UI toggle for "applies to all sites" was built this sprint; that's deferred alongside the already-queued Notification refinements work.
- Caught and fixed a real bug during this sprint's own build, before verification: `_setActive`'s first draft passed `siteId: rule.siteId ?? setBy.siteId`, which would have silently converted an org-wide rule to site-specific the moment anyone deactivated/reactivated it. Fixed to `siteId: rule.siteId` (preserve whatever the rule already had, null included) — now covered by a repository test.
- All three call sites (`end_of_session_summary_screen.dart`'s `_finish`/`_sendToManager`, `notification_rules_screen.dart`'s `_saveRule`/`_setActive`) already held a site-bearing `User`, same as 015c — `currentSiteProvider` still hasn't been needed by any cluster after 015b's wizard.

## Notification firing (Sprint 016) — resumes the sprint paused for the multi-site foundation
- No `Notification`/`TriggerNotification` entity existed before this sprint — Sprint 014 built only `NotificationRule` (the configuration). Confirmed this by reading the schema fresh before planning, not assuming from the task description's framing. Built the full stack from scratch: `TriggerNotifications` table, model, repository (`create`/`watchForUser`/`acknowledge` all built now even though the read/acknowledge sides sit unused until the inbox sprint).
- `TaskSubmissionRepository.submit()` changed from `Future<void>` to `Future<int>` (surfacing the already-computed inserted row id) — needed so a `TriggerNotification` can link back to the submission that caused it. This touched a file outside the originally-approved list; flagged and confirmed before building rather than added unilaterally.
- Matching: active `NotificationRules` where `taskTemplateGroupId` is null or matches the failed task, AND `siteId` is null (org-wide) or matches the submission's site.
- Precedence: matching rules are grouped by their exact `taskTemplateGroupId` (null/"any" is its own group). Within a group, an active top-tier rule suppresses mid-tier rules in that same group — this is the concrete algorithm chosen to implement PROJECT_BIBLE's "top tier can override mid-tier notification settings," confirmed with the user since no exact algorithm was ever specified.
- Fan-out: a rule targeting a specific user creates one `TriggerNotification`; a rule targeting a role tier fans out to every user in that tier — scoped to the submission's site if the rule is site-specific, or across all sites if the rule is org-wide (null `siteId`).
- `notificationRuleId` on `TriggerNotification` references the specific rule *version* active when it fired, not the `ruleGroupId` — preserves exactly what config caused the notification even if the rule is edited later, matching this project's immutable-audit-trail philosophy.
- `channelPush`/`channelEmail` still do nothing — no backend exists. Carrying forward Sprint 014's "configuration intent only" decision unchanged now that real firing exists.
- `TaskController` gained three new constructor dependencies (`NotificationRuleRepository`, `TriggerNotificationRepository`, `UserRepository`) via the same explicit-DI pattern already used throughout — `task_screen.dart`'s `initState` is the only construction site and was updated accordingly.
- Verified with a repository/controller-level test covering: no rules → no notifications; a site-matching rule fires; a rule scoped to a different site does not fire; a top-tier rule suppresses a mid-tier rule in the same trigger scope; PASS submissions never fire anything; specific-person targeting works independently of tier fan-out. All passed. Also ran a real Windows debug build against the existing dev database confirming the schemaVersion 13→14 migration (new table, no backfill needed) executes cleanly.
- No in-app inbox UI yet — deferred to the next sprint (017), per the agreed two-sprint split. Notifications are being created but nothing surfaces them to a manager/top-tier user yet.

## Notifications inbox (Sprint 017) — completes the 016/017 split
- Exact mirror of Sprint 013's `SessionSummaries` banner pattern: a `StreamBuilder` on `TriggerNotificationRepository.watchForUser(currentUser.id)`, rendering a private `_TriggerNotificationsBanner` (all notifications shown, each with an "Acknowledge" button or a green checkmark once acknowledged).
- Notifications banner placed above the Session Summaries banner on `ManagerScreen` (both banners now stack) — a FAIL that just happened is more time-sensitive than an end-of-shift report. Minor UX ordering call, not flagged as needing separate approval since it's within the already-approved scope.
- `_TriggerNotificationsBanner` is duplicated in both `manager_screen.dart` and `top_screen.dart` rather than extracted to a shared widget — matches this project's established pattern of duplicating small per-screen UI elements (icon buttons, banners) rather than abstracting across ManagerScreen/TopScreen.
- `TopScreen`'s body was restructured from a bare `Center` to a `Column` (banner + `Expanded` placeholder) to accommodate the new banner — the first real body content TopScreen has had beyond its placeholder text, needed to fit this sprint's scope, not a redesign.
- Red-tinted banner (`Colors.red.shade50`) instead of Session Summaries' amber, to visually distinguish the two and match this app's existing red-for-FAIL convention (e.g. the FAIL button on the task screen).
- Given the pumpAndSettle hang encountered in Sprint 014's widget test, this sprint verified the underlying repository behavior directly (`watchForUser` streams only the addressed recipient's notifications; `acknowledge` correctly sets `acknowledged`/`acknowledgedAt`) rather than attempting a full widget test. A real Windows run confirmed both restructured screens render without runtime errors. Interactive visual confirmation of the banner's actual appearance was not independently exercised — left the app running on-device for a manual look, same disclosed limitation as Sprint 014's rule screen.

## Per-task quick-setup notifications (Sprint 018) — first of the queued notification refinements
- Task-template-level granularity, not equipment-instance-level — a row per `TaskTemplate`, matching the schema exactly as Sprint 014 built it. Equipment-instance-level scoping (e.g. "Fridge 1" vs "Fridge 2" separately) was flagged as a real alternative reading of "equipment/task fails" but confirmed out of scope for this sprint — it would need a new `equipmentInstanceId` column plus a `TaskController` matching-logic change, materially bigger than a UI addition.
- All current task templates shown, no filtering by `isCritical`/`requiresCorrectiveActionOnFail` — confirmed, consistent with the existing "Add Rule" form's dropdown which also doesn't filter.
- Sits alongside the Sprint 014 "Add Rule" form, not a replacement — the grid can't express "any task fail" or specific-person targeting, so the form stays for those cases. Rules created either way are ordinary `NotificationRule` rows, indistinguishable in the existing rule list.
- No new repository methods or schema — the grid derives checkbox state by scanning the already-loaded current-version rules for a match on `(taskTemplateGroupId, targetRoleTier, targetUserId: null)`, and toggles via the exact same `saveNewVersion`-with-`ruleGroupId` versioning mechanism `_setActive` already uses. Toggling preserves the existing rule's channels/site if one is found, or defaults to the setter's own site with both channels off if creating fresh.
- Given this exact screen has now hit an unresolved `pumpAndSettle()` hang twice before (Sprints 014 and 017), verified the new toggle logic at the repository level instead — a temporary test replicating the toggle function's exact behavior (not the widget) confirmed: first toggle creates a fresh rule; toggling off then on again reuses the same `ruleGroupId` rather than creating a duplicate group, and preserves the original site. A real Windows run confirmed the screen renders without error; interactive visual confirmation of the grid wasn't independently exercised — left the app running on-device for a manual look.

## Third-party maintenance contacts (Sprint 019) — completes the notification refinements backlog
- A new `ThirdPartyContact` entity, not previously named in ARCHITECTURE_LOCK's Core Entities list — added to that list as part of this sprint (name, company, specialty, phone/email, directory-only, distinct from `User`) rather than left undocumented.
- Directory only, not wired into `NotificationRule` — a contact isn't a `User` (no login, no role tier), so it can't be a `targetUserId`. A manager sees a trigger fire, looks the contact up here, and calls/emails them manually. Confirmed with the user before building, not assumed.
- `specialty` is free text (e.g. "Refrigeration", "Electrical"), not a structured link to `EquipmentType` — avoids a join table for a feature not wired into any automated matching.
- Simple `active` toggle, not full append-only versioning — treated like `Area`/`EquipmentInstance`/`TaskSchedule`, not like `NotificationRule`/`TaskTemplate`. A contact's phone number changing isn't audit-sensitive the way rule precedence is.
- `siteId` nullable, same null-means-org-wide pattern as `NotificationRule.siteId` — a contact can be specific to one site or visible everywhere.
- New screen placed under the existing (previously unused, reserved-for-later per Sprint 003) `lib/features/settings/` folder rather than adding a new top-level folder — avoids amending ARCHITECTURE_LOCK's Folder Structure Rule for a single small screen.
- Add-only + active/inactive toggle, no edit form — matches Sprint 008's established v1-scope precedent for Areas/Equipment.
- Verified with a repository-level test: creating a contact with neither phone nor email throws (mirrors `NotificationRule`'s "must have a target" validation); phone-only and email-only both succeed; org-wide (`siteId: null`) vs. site-specific both store correctly; `setActive` toggles correctly. A real Windows run confirmed the v14→v15 migration executes cleanly against the existing dev database.

## Local data backup/export (Sprint 020) — first feature audit priority
- Raw SQLite file copy, not a structured (JSON/CSV) export — deliberately distinct from PROJECT_BIBLE's separate, later "Inspection data export" item, which serves a different audience (inspectors reading a report) rather than disaster recovery (getting the exact data back onto a replacement device). Confirmed with the user before building, not conflated.
- Implemented via SQLite's `VACUUM INTO ?` (a parameterized `customStatement` on `AppDatabase`) — produces a complete, consistent, compacted snapshot safely while the live connection stays open, avoiding WAL-corruption risk from a naive file copy. Verified with a test that opens the resulting file as an independent sqlite database and confirms it contains real data.
- Export only this sprint, not restore — restore is destructive (overwrites whatever's on the target device) and riskier to do safely while the app has the live file open. Treated as a separate future sprint.
- No file-picker dependency added — the backup auto-saves to a `KitchenControlBackups` folder under the user's Documents (via the already-present `path_provider`/`path` dependencies, both now used directly in app code for the first time) with a timestamped filename, and the resulting path is shown to the manager in a confirmation dialog. Actually moving the file to a USB drive/cloud folder stays a manual step.
- No "last backup" staleness reminder this sprint (would need a small persisted timestamp or a new dependency) and no retention/cleanup of old backup files — both flagged as possible future refinements, not built now.
- Per the user's explicit instruction: the confirmation dialog has an optional "Backup name" text field (e.g. "Pre-inspection backup"); when given, it's folded into the filename *alongside* the timestamp, not in place of it — confirmed the filename always sorts chronologically in a file browser regardless of whether a custom name was given.
- No schema change, no migration, no schemaVersion bump — this is new application code over the existing database, not a new table.
- Dialog logic (`_showBackupDialog`) is duplicated as a private top-level function in both `manager_screen.dart` and `top_screen.dart`, matching this project's established pattern of duplicating small per-screen logic rather than sharing it across ManagerScreen/TopScreen.

## Feature audit priorities (from FEATURE_AUDIT.md, added as new sprints)
1. Data backup/export - local drift DB only lives on one device; losing it loses compliance history. Highest priority.
2. Edit/retire for Areas/Equipment - no way to handle equipment replacement (e.g. broken fridge) without losing submission history
3. Notification escalation on non-acknowledgment - a fired trigger with nobody acting on it defeats the purpose
4. Three task-taxonomy gaps (already logged) - blocking real 100+ task library
5. PIN reset + staff deactivation flow - basic day-one operational need
Full detail and lower-priority items in FEATURE_AUDIT.md (now in project root alongside DECISIONS_LOG.md).

## Standing design rule: custom naming
- Every feature that lets a user create a new entry/element (notification rules, backups, venues/sites, areas, tasks, future entities, etc.) should let them enter and save a custom name for it - not just an auto-derived label. Applies going forward to any new creation feature.
- EXPANDED: naming must also be EDITABLE after creation, not just set once at creation time - a manager should be able to rename something later, not just name it on the way in.
- Real gaps already found:
  - NotificationRule has no name field - identified only by task+tier. Needs a nullable name field.
  - Site/Organisation currently auto-seeded with placeholder names ("My Organisation"/"Main Site") with NO UI to rename them at all. Real gap - a venue should be able to set its own name, not live with a placeholder forever.
  - Areas and EquipmentInstances have names set at creation (venue setup wizard) but are add-only - no edit/rename capability yet (also already flagged in FEATURE_AUDIT.md priority #2).
  - TaskTemplates already have a title field and are versioned, so "editing the name" already works via the existing versioning mechanism - no gap here.
- None of this blocks the in-progress backup/export sprint - these are separate, queued gaps to address when each entity is next touched, not urgent enough alone to justify individual sprints yet. Site/Organisation naming is probably the most user-visible one and worth prioritizing relatively soon.

## Roadmap reshape (after Opus 4.8 strategic review) - branding PAUSED
Branding (was Sprint 026) is paused - it's more top-tier config on a product that isn't yet usable/testable. New near-term order, all aimed at making the app real enough to test with an actual kitchen worker:
- 026: Task presets - "standard task sets" per equipment type and per section, so adding a fryer offers its standard tasks in one tap instead of hand-building each. Done before loading the real library since the library fills these presets.
- 027: Load real 100+ task library from Full_check_list.docx (taxonomy unblocked in Sprint 023), feeding assignment screen + presets.
- 028: Due/overdue tracking - the daily loop, time-of-day awareness, what's due/overdue. Was deferred at Sprint 010; it's the actual core compliance loop and the most important missing piece.
- 029: Visual/UX pass - make it look/feel like a real product (Steve has repeatedly flagged the wireframe look; it's gating real-user testing).
Then resume branding + remaining parked items.
Strategic cautions logged: (1) setup burden is heavy - presets address part of this; (2) local-only architecture (device-clock timestamps, single-device data, any-manager PIN reset) means this is a demo of the loop, not yet a deployable/trustworthy compliance system - that needs the parked backend.

## Parked features (decided, deliberately not built - revisit only when there's real need)
- Regional tier (top->regional->venue mid/base for large multi-branch operators) - schema foundation supports adding this later, but the tier itself isn't built
- Equipment-instance-level notification targeting (per-specific-fridge, not just per task type) - Sprint 018 built the simpler task-level version
- Restore from backup - Sprint 020 built export only, restore deferred to its own future sprint (meaningfully riskier)
- Backup staleness reminder ("haven't backed up in X days") - v1 is a manual button only
- Backup retention/cleanup - no auto-deletion of old backups, manual via Explorer
- Site-switcher UI - not needed until a second real site exists
- Third-party contacts wired into automated notification firing - manual-lookup directory only, not an automated dispatch target
- Brand entity (Organisation -> Brand -> Site) - deferred to whenever real branding work happens
- Task-library site-editing (per-site version of the library vs. one shared library) - real gap against PROJECT_BIBLE wording, accepted as out of scope for the multi-site foundation
- Generic if/then rule engine for tasks - Sprint 007 built minimal structured fields instead, full engine deferred unless a real task needs it
- getAll() siteId-filter parameter on repositories - not needed until there's more than one site to filter by

## Areas/EquipmentInstances rename + retire (Sprint 021a) — first half of closing the add-only gap
- Confirmed directly against ARCHITECTURE_LOCK's Versioning Rule before building: `Area`/`EquipmentInstance` aren't named among the entities requiring append-only versioning (`TaskTemplate`, `LegalLimitReference`, `NotificationRule`, `BrandingConfig`), and aren't task-library configuration in that sense. Rename and retire are both plain mutable `UPDATE`s, consistent with the lock, not a violation — checked, not assumed.
- `EquipmentInstances` had no `active` column at all before this sprint — added (`boolean().withDefault(const Constant(true))()`, a normal `addColumn` with a compile-time default, so existing rows get `true` automatically, no backfill loop needed unlike the earlier nullable-FK `siteId` additions).
- Retiring an equipment instance cascades: it deactivates any `TaskSchedules` currently pointing at that `equipmentInstanceId` (so staff stop being asked to check equipment that no longer exists), while `TaskSubmissions` history is untouched (immutable, never re-validated against current equipment state). Reactivating does **not** restore those schedules — re-assignment is a deliberate, separate manager action via the staff assignment screen. Verified with a repository test covering exactly this asymmetry.
- Retiring shows a confirmation dialog (cascading effect, real consequence); renaming does not (non-destructive). `staff_assignment_screen.dart`'s equipment picker now excludes retired instances from new assignments.
- Retired instances stay visible in the venue setup wizard's Equipment list (greyed, labelled "(retired)") with a Reactivate action, rather than being hidden — matching how inactive rows are already shown elsewhere (`NotificationRulesScreen`, `ThirdPartyContactsScreen`).
- Site/Organisation rename (the second half of the original ask) is deliberately a separate sprint (021b) — different tables, different (currently nonexistent) screen, agreed upfront as a two-sprint split.

## Site/Organisation rename (Sprint 021b) — completes closing the add-only gap
- New `lib/features/settings/venue_details_screen.dart` — the first screen to ever surface Organisation/Site data to a user; previously only read internally via `getDefault()`. Shows the current org/site name each with a rename action — not a list, since only one of each exists in practice today (no multi-site UI yet).
- Same plain-mutable-`UPDATE` `rename()` pattern as Sprint 021a's Areas/EquipmentInstances, for the same reason (checked against ARCHITECTURE_LOCK's Versioning Rule in 021a's planning — neither entity is in the list requiring append-only versioning).
- New "Venue Details" icon added to both `ManagerScreen`/`TopScreen`, placed directly next to the existing "Venue Setup" icon (same logical grouping — venue configuration) rather than at the end of the action list.
- Closes the "Site/Organisation currently auto-seeded with placeholder names... NO UI to rename them at all" gap flagged in the Standing design rule: custom naming section — placeholders can now be renamed to a venue's real name.
- Verified with a repository-level test confirming both the default Organisation and Site rename correctly and the new names persist on re-read. `flutter analyze` clean; no schema change this sprint. A real Windows run confirmed the new screen and icons render without error.

## Notification escalation (Sprint 022) — third feature audit priority
- Fixed global 30-minute threshold, not per-rule configurable — matches this project's repeated "ship the simple version, defer richer config" pattern (Sprint 009's frequency enum, Sprint 020's no-staleness-reminder). Per-rule configurability is a real, separable future refinement, not built now.
- Escalation is both: (a) a brand-new `TriggerNotification` addressed to every top-tier user at the same site, for any unacknowledged notification whose firing rule targeted mid tier or a specific person; and (b) visual re-surfacing (elapsed time + bold/red styling) in the existing banners for any unacknowledged notification past the threshold, regardless of tier.
- Per explicit correction during planning: specific-person-targeted notifications escalate to top tier exactly like mid-tier-targeted ones — a single named recipient not seeing it is exactly the case escalation exists for. Only notifications whose firing rule already targeted top tier get visual-only treatment, since there's nobody further up to escalate to.
- Which category a notification falls into is denormalized onto a new `TriggerNotification.originTargetRoleTier` field at creation time (null for specific-person targets, same as mid-tier) rather than looked up from the rule later — `notificationRuleId` points at the rule version that fired, which may since have a newer version, so this avoids a repository lookup back to a rule the escalation check doesn't otherwise need.
- Mechanism: no backend/background service exists, so escalation is only checked while `ManagerScreen`/`TopScreen` is open in the foreground — a `Timer.periodic` (60s) plus one check on load. Real, disclosed limitation: if nobody has either screen open, nothing escalates. No realistic alternative exists for a purely local app.
- A new `TriggerNotification.escalatedAt` marks a notification once it has triggered an escalation, so the periodic sweep never double-escalates the same one. Pre-existing (pre-Sprint-022) unacknowledged rows migrate with both new columns null, which is treated as escalate-eligible — the conservative default, since a null `originTargetRoleTier` is indistinguishable from (and handled identically to) a specific-person target.
- Escalation fans out to top-tier users at the original notification's own `siteId` (the site where the failure actually happened) — mirrors the existing fan-out site-scoping convention already used when a rule first fires (Sprint 016), rather than introducing a new precedent.

## Task-taxonomy reconciliation (Sprint 023) — fourth feature audit priority
- Priority: added a nullable 3-level `TaskPriority` (`critical`/`high`/`standard`) field alongside the existing `isCritical` boolean, which is left completely untouched — confirmed by grep before building that nothing in the app currently branches on `isCritical` for behavior (corrective-action gating uses the separate `requiresCorrectiveActionOnFail` flag instead), so this reconciliation carried very little risk. Existing rows get `priority: null` on migration (can't be reconstructed from a boolean without guessing High vs. Standard); `TaskTemplate.effectivePriority` falls back to deriving Critical/Standard from `isCritical` for those rows, matching PROJECT_BIBLE's own documented fallback mapping.
- `TaskTemplateRepository.saveNewVersion`'s `required bool isCritical` parameter was replaced with `required TaskPriority priority`, which derives `isCritical: priority == TaskPriority.critical` internally at write time — one source of truth rather than two independently-supplied fields that could desync (the same class of bug caught and fixed in Sprint 015d for `NotificationRule.siteId`). Confirmed as the recommended option before building, not decided unilaterally.
- Method: replaced the custom-task form's 3 placeholder options (`numeric_photo`/`categorical_choice`/`notes_only`) with the checklist's real 8 values (Tick, Data + Tick, Tick + Photo, Data + Photo, Note, Note + Photo, Tick + Note, Multi), stored as the same free-text `method` column — no schema change, since nothing branches on `.method`'s value anywhere in the app (confirmed by grep) and it's only ever set through this one closed dropdown, which already structurally enforces the real vocabulary. Default changed from the old numeric-photo-flavored value to `'tick'`, the simplest real method.
- The one seeded example template ("Check Fridge Temperature") updated to `method: 'data_photo'` (numeric reading + required photo) and `priority: 'critical'` explicitly, matching its existing `isCritical: true`.
- Pre-existing real custom task templates created under the old 3 placeholder `method` strings keep those exact values — no backfill, since nothing displays or branches on them, so there's no visible or behavioral consequence.
- Frequency: expanded `ScheduleFrequency` in place with 7 new values (`threeXDaily`, `perBatch`, `perDelivery`, `perUse`, `twoXPerService`, `eventBased`, `asNeeded`) alongside the existing `daily`/`weekly`/`perShift`/`custom` — no schema change, since the column already stores `.name` as text and existing rows parse identically via `.byName`. Added a `frequencyLabel()` helper so the assignment screen's dropdown shows "3x Daily"/"Per Batch"/etc. instead of raw camelCase (which is what it displayed for the original 4 values too, e.g. "perShift" literally) — applied across all 11 values for consistency.
- Deliberately data-model-only, per the approved plan: `ResolvedTask`/`task_controller.dart`/`task_screen.dart` are unchanged — the new 3-level priority isn't surfaced to staff during task execution this sprint, matching how `isCritical` itself has never been surfaced there. Wiring real behavior on top of priority (e.g. a "CRITICAL" badge, escalation tied to priority) is deferred until something concretely needs it.
- Loading the actual ~100+ task rows from "Full check list.docx" remains a separate, later effort — this sprint only fixes the vocabulary they'll load into.

## PIN reset + staff deactivation (Sprint 024) — fifth feature audit priority, closes out this list
- New `lib/features/settings/staff_management_screen.dart` — fulfils Sprint 003's deferred item ("Real staff CRUD is deferred to a later Settings-feature sprint"). Deliberately separate from `staff_assignment_screen.dart`, whose stated purpose is task assignment, not account administration — matches how Site/Organisation and third-party contacts each got their own `settings/` screen rather than overloading an existing one.
- PIN reset: a manager/top-tier user types a new PIN directly (no old-PIN confirmation), via `UserRepository.resetPin()` — reuses the existing salted-hash machinery from staff creation. Consistent with the existing staff-creation PIN field: no format validation added (staying consistent with what's already there, not scope-creeping into new validation).
- Deactivation: `Users` gains `active` (boolean, default `true`) — confirmed against ARCHITECTURE_LOCK's Versioning Rule first: `User` isn't in the list requiring append-only versioning, so a plain mutable `UPDATE` is correct, same as `Area`/`EquipmentInstance`/`ThirdPartyContact`.
- `Users` also gains `deactivatedAt` and `deactivatedByUserId` (not just a plain boolean) — added per explicit instruction, since staff deactivation is more HR/compliance-sensitive than equipment retirement and is worth having on record. **Confirmed via a direct question rather than assumed:** these two fields are deliberately **preserved on reactivation, not cleared** — "when/who last deactivated this person" stays on record even once they're active again, only overwritten if they're deactivated again later. This is a real behavioral choice, not an obvious default, and differs from how `acknowledgedAt`/`escalatedAt` elsewhere in this app describe only the *current* state.
- Deactivation cascades exactly like Sprint 021a's equipment retire: `UserRepository.setActive()` also deactivates that user's own active `TaskSchedules` (`assignedUserId` match) when deactivating, so a manager doesn't see someone who can't log in as still assigned. Reactivating does **not** restore those schedules — same deliberate asymmetry, same reasoning (re-assignment is a separate manager action).
- Three places needed to actually respect `active`, or this would have real gaps: `staffDirectoryProvider` (login screen's staff list) now filters to active-only; `UserRepository.authenticate()` rejects a correct PIN if the user is inactive (defense in depth, not just a UI-level filter); `staff_assignment_screen.dart`'s staff picker excludes inactive staff from new assignments, mirroring how retired equipment is already excluded there.
- No special handling for a deactivated user who's still the `targetUserId` of a `NotificationRule` or has pending unacknowledged `TriggerNotification`s — confirmed as an accepted, not-fixed-now gap (they simply can no longer log in to see/acknowledge anything addressed to them).
- Verified with a temporary repository-level test (deleted after, not part of this commit): `resetPin` changes the PIN without needing the old one, and the old PIN stops working; deactivating a user blocks login even with the correct PIN, cascades to deactivate their active schedule, and — after reactivating — restores login while the schedule stays deactivated and `deactivatedAt`/`deactivatedByUserId` remain populated (not cleared). `flutter analyze` clean. A real Windows debug run confirmed the schemaVersion 18→19 migration executes cleanly against the existing dev database. Interactive click-through of the new screen's dialogs was not independently exercised — left the app running on-device for a manual look.
- This closes out the full "Feature audit priorities" list (data backup/export, equipment edit/retire, notification escalation, task-taxonomy reconciliation, PIN reset + staff deactivation) — all five are now built. Remaining FEATURE_AUDIT.md items are lower-priority per its own ranking (bulk assignment, due/overdue tracking, multi-site UI, search, admin audit log viewer, etc.), not yet scheduled.

## Create new venue + minimal site context (Sprint 025) — first half of branding/multi-site work, split from Sprint 026 (branding)
- ⚠️ **MULTI-SITE IS NOT FULLY USABLE AFTER THIS SPRINT — READ BEFORE CREATING A SECOND VENUE.** This sprint makes creating a second `Site` *safe* (it no longer silently corrupts the original venue's setup), but does **not** make managing two venues day-to-day *usable*. `Area`/`EquipmentInstance`/`TaskSchedule`/`User` reads (`getAll()`-style queries) remain completely unfiltered by site, exactly as decided in Sprint 015b ("not needed until there's more than one site to filter by") — that condition is now true, and the follow-up work to filter those reads has **not been done yet**. Concretely: once a second venue exists, `staff_assignment_screen.dart`'s staff/equipment pickers, `venue_setup_wizard_screen.dart`'s Area/Equipment lists, and `staff_management_screen.dart`'s staff list will all show **both venues' data mixed together indiscriminately**, with no way to tell which row belongs to which site. Do not create a second real venue expecting day-to-day multi-site operation to work — only do so once the site-filtered-reads follow-up sprint has shipped.
- Found and fixed the specific landmine that made creating a second site actively dangerous rather than just incomplete: `SiteRepository.getDefault()` (and `currentSiteProvider`, which just wraps it) always resolves "the first site by id." `venue_setup_wizard_screen.dart`'s three create call sites (`_addArea`/`_addEquipment`/`_addStaff`) relied on this exclusively — meaning every new area/equipment/staff member created through the wizard would have silently landed on the *original* site forever, no matter which venue a top-tier user thought they were configuring, with zero indication anything was wrong. This was discovered and flagged before building, not worked around silently.
- Minimal fix, not a full site-switcher: new `activeSiteProvider` (`StateProvider<Site?>`, defaults `null`). The wizard's three call sites now resolve `activeSiteProvider ?? currentSiteProvider`'s default — so every existing single-site install behaves byte-for-byte identically (the provider stays null forever unless someone explicitly sets it). Only once a top-tier or mid-tier user explicitly selects a site via `venue_details_screen.dart`'s new "Set as Active" action does new-item attribution follow that selection.
- `venue_details_screen.dart` rebuilt: now lists *all* sites (previously showed only the first/default one), each with rename (available to both tiers, matching Sprint 021b's existing precedent) and a "Set as Active"/"Active" indicator. "Create New Venue" is **top-tier only** — creating a new venue is a company-expansion decision, not a day-to-day mid-tier action; this is the first time this app has scoped a config action to top tier alone rather than mid+top, a deliberate departure confirmed before building (also see Sprint 026's branding screen, which does the same).
- The create-venue dialog itself repeats the "multi-site is partial" warning in-app, at the point of creation — not just in written docs — so a top-tier user can't create a second venue without seeing the limitation first. Reuses `Site`'s existing fields (name, address) — no schema change this sprint.
- `SiteRepository.getDefault()` itself is deliberately left unchanged (still "first by id") — every existing single-site read path continues to work exactly as before; the new `activeSiteProvider` is additive, not a replacement.
- Verified with a temporary repository-level test (deleted after, not part of this commit): creating a second site correctly adds it under the existing Organisation without disturbing the original site, and `getDefault()` still resolves to the original first site afterward (not the new one) — confirming existing single-site call sites are unaffected. `flutter analyze` clean. No schema change, no migration. A real Windows run confirmed the rebuilt Venue Details screen and the wizard's updated call sites render and run without error. Interactive click-through of the create/rename/set-active dialogs was not independently exercised — left the app running on-device for a manual look.
- Deferred to a genuinely separate future sprint: site-filtered reads across `Area`/`EquipmentInstance`/`TaskSchedule`/`User` repositories and their consuming screens — this is what actually makes a second venue usable, not just creatable, and is materially bigger than this sprint (touches most repositories in the app). Branding (Sprint 026) is unrelated to this gap and doesn't depend on it.

## Task presets (Sprint 026) — first of the "Roadmap reshape" testability sprints
- New `TaskPreset` (name, nullable `equipmentTypeId`, nullable `segment`, `active`, `createdByUserId`, `createdAt`) + child `TaskPresetItem` (presetId, `taskTemplateGroupId` soft ref, `defaultFrequency`, `defaultCustomFrequencyDetail`). A "standard task set" — adding a fryer offers its standard tasks in one action instead of hand-building each.
- Not versioned — confirmed against ARCHITECTURE_LOCK's Versioning Rule before building: `TaskPreset` isn't in the list (`TaskTemplate`/`LegalLimitReference`/`NotificationRule`/`BrandingConfig`); it's a curation convenience, not audit-sensitive config, so plain mutable rows (rename, add/remove items, deactivate) are correct — same treatment as `Area`/`EquipmentInstance`/`ThirdPartyContact`. Added `TaskPreset`/`TaskPresetItem` to ARCHITECTURE_LOCK's Core Entities list as part of this sprint.
- Org-wide, no `siteId` — matches `TaskTemplate`/`EquipmentType`/`LegalLimitReference`'s existing unscoped status (Sprint 015a). Site-scoping the preset while its underlying templates stay unscoped would be a real asymmetry.
- A preset requires at least one of `equipmentTypeId`/`segment` (validated in the repository, throws otherwise — mirrors `NotificationRule`/`ThirdPartyContact`'s validation pattern). `taskTemplateGroupId` is a soft reference (same as `TaskSchedule`/`NotificationRule`), so a later template edit applies automatically.
- **Apply lives in the repository, not the widget** — this deviated from the approved plan's "loop in the screen's own orchestration, not new repository logic" wording, so per the standing process rule it was flagged and confirmed via a direct question before building, not decided unilaterally. `TaskPresetRepository.applyPresetToStaff({presetId, staffUserId, equipmentInstanceId?, assignedByUserId, siteId})` creates one `TaskSchedule` per preset item the staff member doesn't already have (dedup on templateGroupId + equipmentInstance, so re-applying is safe) and returns the count created. Writing `taskSchedules` rows directly from this repository mirrors how `EquipmentRepository`/`UserRepository`'s `setActive()` already write to `taskSchedules` directly — an established cross-entity pattern here, not a new one.
- Apply UI is in `staff_assignment_screen.dart` (not the venue setup wizard — `TaskSchedule` needs a staff member, which the wizard has no context for at equipment-creation time). A "Task Presets" section at the top of the assignment list offers each active preset an "Apply" button: a segment-only preset applies directly (no instance); an equipment-type preset prompts for which matching active instance to apply it to. After applying, the existing schedule checkboxes below refresh to reflect the new assignments, and a snackbar reports the count (or "all already assigned").
- Editable by **mid + top** (icon on both `ManagerScreen` and `TopScreen`) — this is day-to-day setup convenience, matching who already manages `TaskTemplate`/`TaskSchedule`/venue setup, NOT top-tier-only like branding/create-venue.
- New `lib/features/task_library/` folder (added to ARCHITECTURE_LOCK's Folder Structure Rule this sprint) — houses `preset_management_screen.dart`, and is the intended home for Sprint 027's real-library-loading work too, so it's not "a single small screen" the way Sprint 019 justified reusing `settings/`.
- One illustrative preset seeded ("Standard Fridge Tasks", wrapping the existing seeded fridge-temp template) — this project's consistent one-example-per-foundational-sprint precedent. The real preset content arrives with Sprint 027's library load, which populates presets in the same pass (it's already walking each equipment type/segment's real rows). This sprint builds the mechanism only; it does not hardcode real preset content.
- **Follow-up logged (do NOT build yet):** "apply a preset to multiple staff at once" (e.g. six line cooks all needing the standard fryer set). The apply mechanism was deliberately built as a clean per-staff repository method precisely so multi-staff is a loop over `applyPresetToStaff` per staff member — a natural extension, not a rework. Not built now; revisit when there's real need.
- Verified with a temporary repository-level test (deleted after, not part of this commit): the seeded example preset loads with its one item; `create()` throws when given neither equipment type nor segment and succeeds with a segment; `applyPresetToStaff` creates one schedule per item with the item's default frequency, dedups on re-apply (returns 0, no duplicate rows), and returns the created count; `setActive`/`removeItem` behave correctly. `flutter analyze` clean. A real Windows debug run confirmed the schemaVersion 19→20 migration (two new tables) executes cleanly against the existing dev database and the new screen/flow render without error. Interactive click-through of the preset-management and apply dialogs was not independently exercised — left the app running on-device for a manual look.

## Exit behaviour + time-windowed tasks (agreed this session)
EXIT BEHAVIOUR:
- Every screen, every tier, must allow log out / get back - never trapped
- Exiting shift activities mid-way is ALLOWED but never silent: leaving before finishing shows a warning ("Some checks aren't complete. This will be recorded. You can return and finish anytime this shift.") and logs incomplete tasks as not-completed (not vanished, stays outstanding). Preserves compliance (nothing silently skipped) without trapping the user.

TIME-WINDOWED TASKS (anti-cheating - stops e.g. closing tasks done at shift start):
- Each task can have a valid time window; outside it the task is visible but locked ("available from 21:00")
- Timestamp of actual completion is always recorded and is what really matters for audit
- HONEST LIMIT: timestamp is device-clock now (fakeable). Time-windows raise the bar against casual cheating but aren't tamper-proof until a backend provides trusted server time (parked backend work).
- DECISION on how windows are defined: BOTH fixed-clock and relative-to-shift, but SEQUENCED. Fixed-clock times NOW (buildable immediately, covers opening/closing case). Relative-to-shift LATER (needs a real shift-definition system that doesn't exist yet). Task setup supports both so relative is an addition not a rework.

## Build order (agreed)
1. DONE (Sprint 027): Five-tier roles
2. DONE (Sprint 028): Equipment types expanded 18 -> 63
3. DONE (Sprint 029): Venue types + tagging
4. DONE (Sprint 030, Clusters A-F): Load the NEW researched task library (HORECA_TASK_LIBRARY.md, not old checklist) — all 21 segments, ~150 tasks
5. Due/overdue + time-windows + exit behaviour (grouped - all about WHEN tasks happen)
6. Visual/UX pass
Then resume branding + remaining parked items.

## Target market + library research (this session, Opus 4.8)
- Target: ALL HoReCa venue types (hotels, cafes, bars, QSR, restaurants, bakeries, catering, etc.) - option B
- Researched comprehensive task library built: HORECA_TASK_LIBRARY.md (v2, sourced UK figures, limits tagged LAW/FSA/BEST) - REPLACES the old 132-task checklist as the library source
- Researched equipment + venue types: HORECA_EQUIPMENT_AND_VENUES.md - expand 18 -> ~45-55 equipment types; add ~12 venue types as tags to filter tasks/presets at setup (biggest lever against setup burden)
- These SUPERSEDE the old Full_check_list.docx as the content source. Do NOT load the old 132 checklist.

## Tier structure change - THREE TIERS -> FIVE TIERS (agreed)
New five-tier model:
- Base - does tasks, logs proof (KP, commis, line chef, server, bar staff)
- Supervisor - runs a shift, first responder to a failed check on the floor (head chef on shift, duty manager)
- Venue Manager - accountable for one venue, sets up venue, assigns tasks, gets that site's escalations
- Regional - oversees a cluster of venues, gets escalations a venue manager didn't close
- Executive/Director - company-wide, sets policy/branding, sees everything, owns audit relationship
Each tier = a distinct escalation/visibility boundary; a problem rolls up one level. Job titles are labels mapped onto tiers. SUPERSEDES the earlier "regional tier parked" decision. Touches every permission check/escalation/dashboard (all assume 3 tiers today). Current app still has 3 tiers built.

## Five-tier roles (Sprint 027) — first item of the "Build order," supersedes three-tier roles (Sprint 006)
- `RoleTier` changed from `{top, mid, base}` to `{base, supervisor, venueManager, regional, executive}` — declared in that exact rank order, since `tier.index` is now relied on directly as its rank (`roleTierRank`) rather than a separate lookup map; `nextRoleTierUp(tier)` returns the tier one rank above, or null at executive. Both helpers live next to the enum in `user.dart`.
- Old-to-new mapping is genuinely ambiguous, not mechanical: old `mid` ("managers/supervisors") straddles the new Supervisor/Venue Manager split, and old `top` ("C-suite... Area Manager, Operations Manager... Director/MD") straddles the new Regional/Executive split. Recommended-and-agreed default: `mid`→`venueManager`, `top`→`executive` (the closer of each pair's two plausible homes), correctable per-user afterward.
- **`RoleTier` is persisted as a string in five places, not just `Users`** — all five needed the same `mid`/`top` remap in one migration block (schemaVersion 20→21), or `RoleTier.values.byName(...)` would throw the moment it hit old data: `Users.roleTier`, `NotificationRules.targetRoleTier` + `.setByTier`, `TriggerNotifications.originTargetRoleTier`, and `TaskTemplates.applicableRoleTiers` (comma-joined, needed a per-row read-modify-write rather than a `WHERE`-equals `UPDATE` since it's a list, not a single value). Mirrors Sprint 006's exact `UPDATE ... WHERE roleTier = X` approach, not a wipe-and-reseed. Verified against the real existing dev database (not a synthetic test) — the app launched cleanly post-migration, which it couldn't have done if any stored tier string had been left unmapped.
- New `UserRepository.changeRoleTier()` + a "Change Tier" action on `staff_management_screen.dart` — built in this same sprint as a necessary companion to the lossy migration default, not scope creep: without it, nobody could correct a real user who was actually supervisor- or regional-flavored after the automatic remap ran.
- Seed roster expanded from 6 base + 1 mid + 1 top to include one demo user per tier: added Priya Shah (Duty Manager, supervisor) and Marcus Webb (Regional Manager, regional); Jordan Blake (Head Chef/Kitchen Manager) and Alex Rivera (Director/MD) carry forward as venueManager and executive respectively, consistent with the same mid→venueManager/top→executive default used for the migration (not given special-cased treatment based on their job titles, to avoid a fresh install and an upgraded existing install disagreeing on what the same-named seed users' tiers are).
- Five tiers map onto the three existing UI surfaces — no new dashboards built this sprint (explicitly out of scope per the Build Order): `base`→`TaskScreen` (unchanged), `{supervisor, venueManager}`→`ManagerScreen`, `{regional, executive}`→`TopScreen`. Same grouping applied to `task_screen.dart`'s manager-view-icon visibility (now "any tier above base," generalized from the old `mid || top` check) and `end_of_session_summary_screen.dart`'s "send to manager" recipient picker (same generalization).
- Notification firing precedence (`task_controller.dart`) generalized from "an active top-tier rule suppresses mid-tier rules in the same scope" to "rules set by the highest-ranked tier present in a scope suppress rules set by any lower tier in that scope" — a direct extension of Sprint 016's original 2-tier algorithm to all 5, using `roleTierRank`.
- **Escalation behavior changed from Sprint 022's "jump straight to top tier" to "escalate exactly one tier up"** — per this sprint's own tier description ("a problem rolls up one level"), confirmed before building since it changes already-shipped Sprint 022 behavior. For a tier-targeted notification, the rule's own target tier is the reference point; for a specific-person-targeted notification (`originTargetRoleTier` null), there's no rule tier to step up from, so the **recipient's own actual tier** is used as the reference instead — both cases reduce to "escalate one level up from the relevant tier." A notification already at executive is skipped (nothing further up), same "visual-only" treatment as before.
- Notification quick-setup grid (`notification_rules_screen.dart`) expanded from 2 hardcoded Top/Mid checkbox columns to one column per tier (5), horizontally scrollable given the width; short column labels (Base/Supv/Venue Mgr/Regnl/Exec) since the table doesn't have room for full tier names. The "Add Rule" form's tier dropdown needed no change — it already iterated `RoleTier.values` generically.
- "Create New Venue" gate (`venue_details_screen.dart`) changed from top-only to executive-only — a direct continuation of the existing top-only rule, not a scope change.
- `PROJECT_BIBLE.md`'s Role Tier Model section rewritten for the 5-tier structure, replacing the stale 3-tier description.
- Verified with a temporary test (deleted after, not part of this commit): `roleTierRank`/`nextRoleTierUp` match declaration order; the seeded roster represents all 5 tiers; `changeRoleTier` updates the stored tier; a tier-targeted overdue notification escalates exactly one level up and stops at executive; a specific-person-targeted overdue notification escalates one level up from the *recipient's* own tier, not the (nonexistent) rule tier. `flutter analyze` clean. A real Windows debug run against the existing dev database confirmed the schemaVersion 20→21 migration executes without error — the app launching at all is itself proof no stored tier string was left unmapped, since `RoleTier.values.byName` would otherwise throw on the login screen's staff list. Interactive click-through of the new "Change Tier" dialog and the 5-column quick-setup grid was not independently exercised — left the app running on-device for a manual look. The notification-precedence generalization (task_controller.dart) was verified by code review and `flutter analyze` only, not a dedicated multi-tier firing test — it's a mechanical extension of the already-tested Sprint 016 algorithm (equality check → rank-based max), and building a full `TaskController` test harness (8 constructor dependencies) for this one additional case wasn't judged worth the cost given everything else this sprint verified directly.
- Deferred, explicitly out of scope per the Build Order: distinct dashboards per tier (Supervisor/Venue Manager currently share identical UI, as do Regional/Executive) — revisit once there's real need to differentiate them. Per-rule configurable escalation behavior. Equipment type expansion (18→~50) and venue-type tagging are Build Order items 2 and 3, not this sprint.

## Equipment type library expansion (Sprint 028) — second item of the "Build order"
- Expanded `EquipmentType` seed list from 18 to 63, from `HORECA_EQUIPMENT_AND_VENUES.md` Part A — covers the gaps identified there for the ALL-HoReCa target market: beverage (coffee machine, glasswasher, cellar cooler, keg system, water boiler, etc.), prep machinery (planetary mixer, food processor, slicer, mincer, vacuum packer), bakery (deck oven, proving cabinet, dough sheeter), cold-storage variants (prep fridge, undercounter fridge, serve-over fridge, refrigerated display case, back-bar fridge, gelato dipping cabinet), plus wash-up, ventilation/safety, and non-refrigerated storage additions.
- Same mechanism as Sprint 011's original expansion — `_ensureExpandedEquipmentTypes()` runs unconditionally on every `beforeOpen` (not gated on "table empty"), inserting only names not already present. No schema change, no schemaVersion bump, no migration — pure reference-data growth, same as Sprint 011.
- **Cross-referenced the doc's own "Bold = likely already in your 18" / "(NEW)" markings against the real seed list rather than trusting them, per the Hard Rule against guessing — found two discrepancies**: "Walk-in freezer" was marked `(NEW)` in the doc but is already in the original 18 (skipped here, not duplicated under a slightly different name); "Display / serve-over fridge" was marked bold (assumed already present) but genuinely wasn't (added here as "Serve-Over Fridge"). Neither is a conflict needing a decision — just a factual correction made by checking the actual code, not the doc's guess about it.
- New names condensed to Title Case, no slashes or parenthetical asides, matching the existing 18's style (e.g. the doc's "Prep/counter fridge (refrigerated prep table / saladette)" → "Prep Fridge"). A few of the doc's items were kept as separate distinct types rather than merged despite conceptual overlap (e.g. "Deck Oven" and "Pizza Oven" both listed, since the doc itself lists them as separate real equipment categories) — followed the doc's own item breakdown faithfully rather than second-guessing which to consolidate.
- The resulting total (63) is somewhat higher than the doc's own rough summary line ("~45-55" / "roughly 30-40 more") — its detailed Part A list itemizes 45 genuinely new types once cross-checked against the real 18, which is more than its own summary estimated. Treated the detailed list as authoritative over the summary's rough count, since the user's task explicitly asked for "the fuller set in Part A," not a specific headcount.
- No `EquipmentType` category/grouping field added, despite Part A's own categorization into 8 groups (cold storage, hot line, holding, prep, wash-up, beverage, ventilation, storage) — the existing type picker is a flat dropdown with no category concept, and narrowing that list is explicitly Build Order item 3 (venue-type tagging), not this sprint. Kept this sprint to a straight data addition, per the task's own scoping.
- Part B of the doc (venue types + segment applicability matrix) was read for context but not acted on — that's Build Order item 3, a separate future sprint.
- Verified with a temporary test (deleted after, not part of this commit): a fresh install seeds exactly 63 equipment types with no duplicate names, including spot-checks of both original and newly-added names; an existing install manually reduced to only the original 3 types gets backfilled to the full 63 (not gated on which 3 it started with) without duplicating those 3, confirmed by closing and reopening the database connection (matching how the real app re-runs this check on every launch) rather than exposing a private method for testing. `flutter analyze` clean. A real Windows debug run confirmed the backfill executes without error against the existing dev database (no schemaVersion bump to verify, since none was needed).

## Venue types + tagging (Sprint 029) — third item of the "Build order"
- New `VenueType` reference table (id, name only) seeded with the 12 canonical types from `HORECA_EQUIPMENT_AND_VENUES.md` Part B (Quick Service (QSR), Fast Casual, Casual Dining, Fine Dining, Café, Bakery / Patisserie, Bar / Pub, Gastropub, Hotel, Contract / Institutional Catering, Event / Mobile / Street Food, Dark / Ghost Kitchen) — same always-ensured idempotent seeding mechanism as `EquipmentType` (Sprint 011/028): checked by name on every `beforeOpen`, only missing names inserted. Fixed list, no `active`/retire flag, same as `EquipmentType` — managers extend it via a "Something else..." inline-create escape hatch (also matching `EquipmentType` precedent) rather than editing/retiring the canonical list.
- Four new many-to-many join tables, all schemaVersion 21→22 in one migration block: `SiteVenueTypes` (real FK both sides), `EquipmentTypeVenueTypes`, `TaskPresetVenueTypes` (real FK both sides), and `TaskTemplateVenueTypes` (keyed on `taskTemplateGroupId`, a soft reference — same pattern as `TaskSchedule`/`NotificationRule`/`TaskPresetItem`'s existing soft references to template groups, since `templateGroupId` has no unique constraint and is shared across a template's version rows).
- **Only `SiteVenueTypes` is wired into real UI this sprint** — a site can have more than one type (e.g. a gastropub is kitchen + bar), so tagging is a `FilterChip` multi-select (plus a "Something else..." `ActionChip` for inline custom-type creation) added under each site card on `venue_details_screen.dart`, with instant-toggle save semantics (each chip tap writes immediately via `SiteRepository.setVenueTypeIds()`, no separate Save button). Not added to the Venue Setup wizard — Venue Details is the existing single home for site-level metadata (name, address), matching Sprint 021b's precedent.
- Per the doc's own explicit recommendation, this is a **tagging aid, not a hard lockout**: nothing in this sprint restricts which tasks/presets/equipment a venue can add based on its tagged type(s) — that filtering-at-setup-time behavior is deliberately not built yet (see below), and even once built, a café that happens to have a fryer will still be able to add fryer tasks manually.
- `EquipmentTypeVenueTypes` and `TaskPresetVenueTypes` are **schema + repository methods only this sprint** (`getVenueTypeIds`/`setVenueTypeIds` on `EquipmentRepository`/`TaskPresetRepository`) — no seeded tag data, and not wired into the venue setup wizard's equipment picker or the presets section. `HORECA_EQUIPMENT_AND_VENUES.md` Part B only gives broad segment-level applicability guidance (a matrix covering 8 of the 12 venue types across 15 segments), not sourced per-equipment or per-preset venue-type data — tagging real rows with real venue types is deferred until that data exists, rather than guessing.
- `TaskTemplateVenueTypes` is likewise **schema + repository method only** (`getVenueTypeIds`/`setVenueTypeIds` on `TaskTemplateRepository`, keyed on `templateGroupId`) and deliberately **unpopulated and unwired** — this is explicit groundwork for Build Order item 4 (loading `HORECA_TASK_LIBRARY.md`), which is expected to populate real per-task venue-type tags as it loads each task row, rather than this sprint guessing at tags for the one seeded example template.
- Kept as one sprint (~11 files: schema, model, repository, providers, one screen), not split — matches this session's precedent for schema-plus-one-UI-surface sprints (e.g. Sprint 021a/b).
- Verified with a temporary repository-level test (deleted after, not part of this commit): the 12 canonical venue types seed with no duplicates and don't re-duplicate on reopen (closing and reopening a real `AppDatabase()` against the same temp file, matching how the real app re-runs `beforeOpen` on every launch — not a private-method test hook); `SiteRepository.setVenueTypeIds`/`getVenueTypeIds` support multi-type tagging, replace-all semantics (not additive), and clearing to empty; custom venue-type creation via the repository; the three unwired join tables' get/set methods round-trip correctly, including confirming `TaskTemplateVenueTypes` tags survive a new template version being saved under the same `templateGroupId` (proving the soft-reference key choice works as intended). `flutter analyze` clean. A real Windows debug run against the existing dev database confirmed the schemaVersion 21→22 migration executes cleanly, and manual click-through confirmed the Venue Details chip UI renders, toggles, and persists correctly — independently exercised this sprint, not left as a disclosed gap.

## Task library load: Cluster A (Sprint 030) — first of 6 clusters for Build Order item 4
Plan for loading HORECA_TASK_LIBRARY.md was proposed and agreed in full before building (field mapping, limits/basis storage, venue-type tagging heuristic, idempotent seeding, 6-cluster split, 11 open decisions — all agreed with the stated recommended defaults). This entry records what Cluster A (Segments 1-4: Food Safety & Temp, Allergen Management, Personal Hygiene & PPE, Refrigeration & Cold Storage equipment condition) actually built, plus one plan deviation caught and confirmed mid-build.
- **Plan deviation, flagged and confirmed before building, per the standing process rule**: the approved plan only listed `twoXDaily`/`monthly` as new `ScheduleFrequency` gaps. Building Cluster A surfaced a third: plain "Per service" (distinct from "Per shift" and "2x per service") appears in this very cluster ("Display/serve-over fridge temperature — Per service"). Added `perService` as a third new value (14 total) rather than silently folding it into `perShift`/`custom` — confirmed via direct question before implementing.
- `LegalLimitReferences` altered for the first time since Sprint 007: new required `basis` column (`'law'`/`'fsa'`/`'best'`, compile-time default `'fsa'`), plus nullable `verifiedAt`/`verifiedByUserId` (unpopulated — forward-compatible groundwork for a future professional-sign-off sprint, no UI to set them yet). schemaVersion 22→23. Migration backfills the 3 pre-existing rows to their real basis (`fridge_temp`→law, `freezer_temp`→fsa, `hot_hold_temp`→law) rather than leaving them on the generic default; `_seedTaskLibraryReferenceData` (the fresh-install path) updated to pass the same explicit values so a brand-new install and a migrated one agree.
- 4 new `LegalLimitReference` rows added (idempotent, checked by category via a new `_ensureLegalLimitReference` helper): `cooked_core_temp`/`reheated_core_temp` (70°C min, [FSA] — England/Wales/NI figure; Scotland's stricter 75°C/30s and 82°C [LAW] figures noted in `fixInstructions` text only, not modeled as real per-site jurisdiction data — accepted limitation per the agreed plan), `cooling_temp` (8°C max, [FSA], "90-minute rule" noted in text since duration isn't modeled), `defrost_temp` (8°C max, [FSA]).
- Where the source gives both a hard [LAW]/[FSA] limit and a softer "target" figure (e.g. fridge 8°C legal max + 5°C FSA target), only the hard limit became the structured `minLimit`/`maxLimit` — the schema has no second-threshold concept, so the target and its tag went into `fixInstructions` text instead, per the agreed plan.
- 36 new `TaskTemplate`s loaded (idempotent, checked by exact title before inserting — the by-name pattern established for `EquipmentType`/`VenueType`, adapted for a versioned entity). `Base`/`Mid` role tags mapped to `[base]` / `[supervisor, venueManager]` respectively (the agreed paired-group default, not Sprint 027's single-tier migration mapping). One interpretive equipment-mapping call: "Display/serve-over fridge temperature" mapped to the more specific "Serve-Over Fridge" type (added in Sprint 028 anticipating exactly this) rather than the source row's terser "Fridge" tag — favoring the task's own title over the column's shorthand.
- 9 new `TaskPreset`s generated per the agreed rule (equipment-tagged tasks → that equipment's preset; equipment-less tasks → their segment's preset, so no task appears twice): Fridge/Freezer/Walk-in Fridge/Blast Chiller/Serve-Over Fridge/Bain-marie Tasks (6 equipment-type presets) plus Food Safety/Allergen Management/Personal Hygiene & PPE Tasks (3 segment presets). Named distinctly from Sprint 026's "Standard Fridge Tasks" so the two coexist rather than colliding on the by-name idempotency check — that original illustrative example is deliberately left in place (Open Decision 9), accepting minor duplication.
- `TaskTemplateVenueTypes` populated for all 36 tasks; `TaskPresetVenueTypes` populated for all 9 new presets as the union of member tasks' tags (an addition beyond the literal ask, flagged in the plan as worth doing since Sprint 029 built the table specifically for this). Per the agreed matrix-mapping heuristic, all 4 of Cluster A's segments resolve to all 12 venue types (Personal Hygiene/PPE is check-marked universally in the matrix; Food Safety and Allergen include a "sometimes" for Bar/Pub, which still counts as tagged since tags aren't a lockout; Refrigeration & Cold Storage has no separate matrix row so it's aliased to Food Safety's). This means Cluster A itself produces no venue-type differentiation — expected, not a bug; filtering value shows up starting with more venue-specific clusters (Bar & Beverage, Hotel-Specific, Fryer/Oil, Front of House).
- Method vocabulary extended from 8 to 10 values: added `data` (plain numeric reading, needed by Cluster A's "Blast chiller cycle temperature") and `data_note` (not needed until a later cluster, added now to avoid a second UI touch). Both added to the custom-task form's method dropdown in `staff_assignment_screen.dart`.
- New persistent banner on `preset_management_screen.dart` (Icon + text, no emoji per this project's UI convention) stating task limits are researched/sourced and tagged [LAW]/[FSA]/[BEST] in each task's `fixInstructions`, but not yet signed off by a qualified food-safety professional — the cheap, immediate half of Open Decision 10; the schema-side half (`verifiedAt`/`verifiedByUserId`) ships this sprint unpopulated, with no UI to set them yet.
- `PROJECT_BIBLE.md`'s stale "Task Library Source Content" section (old 19-segment/8-method taxonomy) is deliberately **not** rewritten this cluster — deferred to the final cluster (F), once all 21 segments and the full method/frequency vocabulary are known, rather than rewriting it incrementally 6 times.
- Verified with a temporary repository-level test (deleted after, not part of this commit): exactly 37 current-version templates (36 new + the Sprint 007 example) with no duplicate titles; re-seeding on reopen doesn't duplicate templates or presets (10 total, matching real `AppDatabase()` reopened against the same temp file); legal limit basis correct for all 7 categories (3 backfilled + 4 new); role-tier mapping correct for both a Base and a Mid-tagged task; equipment mapping resolves real seeded ids including the Serve-Over Fridge judgment call, and probe-tagged tasks correctly get a null equipment type; all 9 presets have correct item counts; venue-type tagging covers all 12 types for both a task and its containing preset; the 3 new `ScheduleFrequency` values (`twoXDaily`, `perService`, `monthly`) are stored and read back correctly. `flutter analyze` clean. A real Windows debug run against the existing dev database confirmed the schemaVersion 22→23 migration executes cleanly; manual click-through confirmed the Preset Management screen's new presets/banner and the custom-task form's new method options render correctly.
- Remaining clusters (B: Cooking Line Equipment/Wash-up; C: Cleaning/Chemicals/Dry Storage/Deliveries; D: Utilities & Safety/Waste & Pest/Maintenance/Stock Control; E: Opening/Closing/Service Readiness; F: Front of House/Bar & Beverage/Hotel-Specific/Management Oversight) not yet started.

## Task library load: Cluster B (Sprint 030 follow-up) — second of 6 clusters for Build Order item 4
Segments 5-6: Cooking Line Equipment (5.1 Fryer & Oil, 5.2 Ovens/Grills/Hobs/Other, 5.3 Mechanical & Safety) and Wash-up/Dishwash. Same approach as Cluster A throughout (idempotent by-title/by-name seeding, LAW/FSA/BEST basis in `fixInstructions`, `Base`/`Mid`→`[base]`/`[supervisor, venueManager]` mapping, equipment-tagged-vs-segment-tagged preset rule). No new method or frequency vocabulary gaps this cluster — every value used already existed after Cluster A.
- 22 new `TaskTemplate`s, 12 new `TaskPreset`s (several single-item, e.g. Grill/Hob/Rotisserie/Steamer/Extraction Canopy/Gas Interlock System/Glasswasher Tasks — matches Cluster A's precedent of single-item presets being expected, not an error).
- 4 new `LegalLimitReference` rows, all `[BEST]` (no UK legal limit exists for any of them): fryer oil temperature (≤180°C) and oil TPM% (discard ~24-27%, structured as maxLimit 24.0), dishwasher wash temperature (55-65°C) and rinse temperature (≥82°C).
- **Two decisions confirmed before building, per the standing process rule — the venue-tagging heuristic needed real judgment calls for the first time (Cluster A's 4 segments happened to all be universal/near-universal, so this never came up before):**
  1. The applicability matrix gives Fryer/Oil (segment 5.1) a stricter row than general Cooking Line Equipment (5.2/5.3) — explicit ✗ for Bar/Pub on Fryer/Oil specifically, vs. only "sometimes" on the general row. Confirmed: keep segment-level tagging simple — all of Segment 5 (including fryer/oil tasks) uses the single, more permissive "Cooking line equip" tag set, rather than splitting the segment into a fryer-specific sub-tag. Accepted trade-off: Bar/Pub gets tagged for fryer tasks it may not need, but tags aren't a lockout so the real-world cost is low (no Fryer equipment instance means no fryer tasks trigger regardless of the tag).
  2. Does 'Event / Mobile / Street Food' get tagged for Wash-up/Dishwash? The matrix marks Wash-up as fully universal (✓ with no exceptions), but this venue type's reduced subset (defined in the original Sprint 030 plan as "food safety, hygiene, cleaning, deliveries, waste only") didn't explicitly include wash-up. Confirmed: exclude it, staying within the originally-stated reduced subset rather than silently expanding it. Both `cooking_line_equipment` and `washup_dishwash` end up tagging all 12 venue types **except** Event/Mobile/Street Food (11 of 12) — the first cluster where venue-type tagging actually differentiates between venue types, as expected once past Cluster A's uniformly-universal segments.
- Four equipment-mapping judgment calls, extending Cluster A's Serve-Over Fridge precedent (a task's own specific subject overrides a blank or generic column tag when a matching seeded type exists) — disclosed, not separately confirmed, since they're low-risk mechanical calls of the same kind Cluster A already established: "Extraction canopy filters clean" (column blank) → Extraction Canopy; "Gas interlock / emergency cut-off test" (column blank) → Gas Interlock System; "Glasswasher functioning & dosed" (column says the generic "Dishwasher", title names Glasswasher specifically) → Glasswasher. Conversely, where a title names two distinct equipment concepts and the column tags only one, the literal column tag was followed rather than guessing which was meant: "Rotisserie / kebab machine..." stays Rotisserie only (not also Kebab Machine); "Grill / salamander..." stays Grill only (not also Salamander) — a new, explicit counterpart rule to the "expand when unambiguous" cases above.
- Verified with a temporary repository-level test (deleted after, not part of this commit): exactly 59 current-version templates (37 from Cluster A/example + 22 new) with no duplicate titles; re-seeding on reopen doesn't duplicate templates or presets (22 total); legal limit basis correct for all 4 new `[BEST]` categories; all 4 equipment-mapping judgment calls resolve correctly, including confirming Rotisserie/Grill were NOT over-expanded to Kebab Machine/Salamander; role-tier mapping correct (only the Gas Interlock task is Mid-tagged); preset membership counts correct including single-item presets; venue-type tagging covers exactly 11 of 12 types (excluding Event/Mobile/Street Food) for both segments. `flutter analyze` clean — no schema change this cluster (pure data addition, `app_database.g.dart` confirmed unchanged, matching Sprint 028's precedent for data-only additions). A real Windows debug run against the existing dev database confirmed the app runs without error.
- Remaining clusters (C: Cleaning/Chemicals/Dry Storage/Deliveries; D: Utilities & Safety/Waste & Pest/Maintenance/Stock Control; E: Opening/Closing/Service Readiness; F: Front of House/Bar & Beverage/Hotel-Specific/Management Oversight) not yet started.

## Task library load: Cluster C (Sprint 030 follow-up) — third of 6 clusters for Build Order item 4
Segments 7-10: Cleaning & Sanitation (7.1 Food-Contact Surfaces & Equipment, 7.2 Floors/Walls/Drains, 7.3 Schedule Verification), Cleaning Chemicals & Consumables, Dry & Ambient Storage, Deliveries & Goods In. Same approach as Clusters A/B throughout. No new method or frequency vocabulary gaps this cluster — every value used already existed after Cluster A.
- 28 new `TaskTemplate`s, 6 new `TaskPreset`s (2 equipment-type: Ice Machine, Dry Store Area; 4 segment: Cleaning & Sanitation, Cleaning Chemicals, Dry & Ambient Storage, Deliveries & Goods In).
- 2 new `LegalLimitReference` rows, both `[FSA]`: chilled delivery temperature (≤8°C, aim 5°C) and frozen delivery temperature (≈-18°C) — distinct categories from the existing `fridge_temp`/`freezer_temp`, since a delivery can be in-spec once stored but out-of-spec on arrival, and the goods-in check needs its own reference point.
- **Two venue-tagging decisions confirmed before building, per the standing process rule** — segments 8 (Cleaning Chemicals) and 9 (Dry & Ambient Storage) have no separate matrix row, and Event/Mobile/Street Food's originally-defined reduced subset ("food safety, hygiene, cleaning, deliveries, waste") didn't unambiguously resolve either:
  1. Cleaning Chemicals & Consumables (segment 8): confirmed **include** Event/Mobile/Street Food, since chemicals are tightly coupled to the cleaning tasks already in its reduced subset (unlike the looser fryer/wash-up pairing from Cluster B) — segment tags all 12 venue types.
  2. Dry & Ambient Storage (segment 9): confirmed **exclude** Event/Mobile/Street Food, staying within the originally-defined reduced subset and consistent with the doc's own "compact, portable" framing for this venue type — segment tags 11 of 12 (reuses Cluster B's `_clusterBVenueTypeNames` constant).
  Segments 7 (Cleaning & Sanitation) and 10 (Deliveries & Goods In) both have fully universal matrix rows (✓ across every column, no exceptions) and are both explicitly named in the reduced subset, so both tag all 12 with no ambiguity.
- Equipment-mapping notes, extending the established rules: "Dry store temperature / humidity" (column blank, title unambiguous) → Dry Store Area, same pattern as Cluster B's Extraction Canopy/Gas Interlock System. Two NEW left deliberately unmapped: "Slicer / mincer strip-down clean" (column blank, but title names TWO real seeded types — Slicer and Mincer — with no signal preferring one, a new sub-case of the "don't guess between two named options" rule, previously only seen with a populated-but-partial column in Cluster B); "Prep surfaces cleaned & sanitised" (column blank, but "prep surfaces" reads as a general food-contact-surface concept, not obviously the same thing as the seeded "Prep Station" equipment type — mapping it there would be inferring an equivalence the source doesn't state).
- Verified with a temporary repository-level test (deleted after, not part of this commit): exactly 87 current-version templates (59 from Clusters A/B/example + 28 new) with no duplicate titles; re-seeding on reopen doesn't duplicate templates or presets (28 total); legal limit basis correct for both new `[FSA]` delivery categories; equipment mapping resolves Dry Store Area correctly and confirms Slicer/mincer and Prep surfaces are correctly left unmapped; role-tier mapping correct for Mid-tagged tasks (COSHH sheets, supplier traceability); preset membership counts correct; venue-type tagging covers exactly 12 of 12 for segments 7/8/10 and 11 of 12 for segment 9. `flutter analyze` clean — no schema change this cluster, `app_database.g.dart` confirmed unchanged.
- **Test environment note**: the verification test initially failed 3 of 7 checks by timeout (not assertion failure) on the first two attempts. Root-caused to the machine's C: drive having only ~180 MB free — each test spins up a fresh `AppDatabase()` re-running the full, now-87-template seed pipeline from scratch, and the later, heavier tests (presets, venue-tagging) were pushing past the default 30s per-test timeout under that disk pressure. The user freed space (up to ~527 MB free); a rerun still showed the identical 4-pass/3-timeout split, confirming it wasn't disk space alone — re-running with `flutter test --timeout=120s` then passed all 7 cleanly, confirming this was a timeout-ceiling issue from the growing seed pipeline's cumulative runtime, not a logic bug. Worth keeping in mind for Clusters D-F: the per-test default timeout may need explicit extension again as the seeded dataset keeps growing.
- A real Windows debug run against the existing dev database confirmed the app runs without error; manual click-through (independently exercised by the user) confirmed the new Cluster C presets appear correctly in Preset Management.
- Remaining clusters (D: Utilities & Safety/Waste & Pest/Maintenance/Stock Control; E: Opening/Closing/Service Readiness; F: Front of House/Bar & Beverage/Hotel-Specific/Management Oversight) not yet started.

## Task library load: Cluster D (Sprint 030 follow-up) — fourth of 6 clusters for Build Order item 4
Segments 11-14: Utilities & Safety, Waste & Pest Control, Preventive Maintenance, Stock Control. Same approach as Clusters A/B/C throughout. No new method vocabulary — `data_note` (added in Cluster A but unused until now) gets its first real use ("Wastage / spoilage log"). No new frequency values — "Per visit" (pest control contractor visit) folds into `eventBased`, per the original Sprint 030 plan's already-agreed folding of Per order/Per menu change/Per visit into that one value (not a new gap, just the first time it actually came up).
- 23 new `TaskTemplate`s, 5 new `TaskPreset`s plus one existing preset (Extraction Canopy Tasks, from Cluster B) gaining a second item.
- **First task in the whole library tagged `Top`** ("Gas safety certificate in date") — confirmed before building: `Top` maps to `[regional, executive]`, mirroring the established `Mid`→`[supervisor, venueManager]` default's pairing to Sprint 027's screen groupings (Top pairs to the TopScreen grouping the same way).
- **Three venue-tagging decisions confirmed before building, per the standing process rule** — segments 11 (Utilities & Safety), 13 (Preventive Maintenance), and 14 (Stock Control) have no separate matrix row, and none were explicitly named in Event/Mobile/Street Food's original reduced subset:
  1. Utilities & Safety: confirmed **include** — the source doc's own venue description for Event/Mobile/Street Food explicitly names "handwash challenges," which is literally part of this segment. Tags all 12.
  2. Preventive Maintenance: confirmed **exclude** — reads as fixed-premises compliance concepts (certificates, contracted inspections tied to a location) that don't naturally fit a mobile/temporary setup, and wasn't in the original subset. Tags 11 of 12 (reuses `_clusterBVenueTypeNames`).
  3. Stock Control: confirmed **include** — closely related to Deliveries (already in scope) and universal in practice even for mobile operations. Tags all 12.
  Segment 12 (Waste & Pest Control) has a matrix row and "waste" was explicitly named in the original subset — no ambiguity, tags all 12.
- **Equipment-mapping note — the first case of two tasks across different clusters mapping to the same equipment type**: "Extraction/duct professional clean in date" (this cluster) maps to Extraction Canopy, the same type Cluster B's "Extraction canopy filters clean" already uses. Since `TaskPreset`s are matched by exact name and "Extraction Canopy Tasks" already exists from Cluster B, the previous by-name-skip-if-exists preset logic would have silently dropped this new task from any preset. **Extended the preset-seeding loop** (not a new judgment call — a necessary, mechanical fix to preserve the existing "equipment-tagged tasks bundle into that equipment's preset" rule for a case that hadn't come up before): it now resolves-or-creates a preset by name, then adds any of the current cluster's items the preset doesn't already have (checked by `templateGroupId`) and any venue-type tags it doesn't already carry — all idempotent, safe on reseed. Clusters A/B/C's own seed functions are untouched; each cluster's function remains an independent, self-contained copy, matching the established pattern of not sharing/refactoring seed logic across clusters.
- "Hand-wash sinks stocked" (column blank, title unambiguous) → Hand-Wash Sink, same established pattern as Cluster B/C's blank-column cases. "PAT / electrical inspection in date" and "Gas safety certificate in date" left deliberately unmapped — both are venue-wide compliance checks (PAT covers all electrical equipment generally; the gas certificate covers the overall gas installation, not the Gas Interlock System specifically), not a single named equipment instance.
- Verified with a temporary repository-level test (deleted after, not part of this commit, run with an explicit 90s per-test timeout from the start given Cluster C's disk-space/cumulative-runtime finding — confirmed the machine had ~5.5 GB free before running, well clear of that issue this time): exactly 110 current-version templates (87 from Clusters A/B/C/example + 23 new) with no duplicate titles; re-seeding on reopen doesn't duplicate templates or presets (33 total — 28 existing + 5 genuinely new, since Extraction Canopy Tasks isn't counted as new); Top-tier mapping correct; equipment mapping resolves Hand-Wash Sink/Extraction Canopy correctly and confirms PAT/Gas cert are left unmapped; the Extraction Canopy Tasks preset gains exactly one new item (2 total: Cluster B's weekly filter clean + this cluster's monthly duct clean) without creating a duplicate preset, and doesn't duplicate that item on reseed; all 5 new presets have correct item counts; venue-type tagging covers exactly 12 of 12 for utilities/waste/stock and 11 of 12 for preventive maintenance. `flutter analyze` clean — no schema change this cluster, `app_database.g.dart` confirmed unchanged.
- Remaining clusters (E: Opening/Closing/Service Readiness; F: Front of House/Bar & Beverage/Hotel-Specific/Management Oversight) not yet started.

## Task library load: Cluster E (Sprint 030 follow-up) — fifth of 6 clusters for Build Order item 4
Segments 15-17: Opening Procedures, Closing Procedures, Service Readiness. Same approach as Clusters A-D throughout. No new method, frequency, or role-tier vocabulary this cluster — every value used already existed.
- 13 new `TaskTemplate`s, 3 new segment `TaskPreset`s (Opening/Closing/Service Readiness Tasks), plus two existing presets from Cluster A gaining a second item each — Bain-marie Tasks and Fridge Tasks — using the resolve-or-create-then-merge preset loop Cluster D introduced, per explicit instruction to follow that pattern rather than needing to re-derive it.
- "Hot-hold / bain-marie pre-heated" reuses the existing `hot_hold_temp` `LegalLimitReference` category (Cluster A's ≥63°C [LAW] figure) rather than a new one — it's the same real-world legal threshold, checked at a different moment (before service starts, not during it). No new `LegalLimitReference` rows this cluster.
- **One venue-tagging decision confirmed before building, per the standing process rule** — all three segments have no matrix row and weren't named in Event/Mobile/Street Food's original reduced subset ("food safety, hygiene, cleaning, deliveries, waste"). Confirmed **include** all three: opening/closing/service-readiness are operational/temporal concepts (a trading day's start, end, and service-prep) relevant to any venue with a service period, including mobile ones — a street food van still opens, closes, and preps each day/event. All three segments tag all 12 venue types (first time all three ambiguous segments in a cluster resolved the same way).
- Verified with a temporary repository-level test (deleted after, not part of this commit, run with an explicit 90s per-test timeout from the start, disk space confirmed ~5.1 GB free beforehand): exactly 123 current-version templates (110 from Clusters A-D/example + 13 new) with no duplicate titles; re-seeding on reopen doesn't duplicate templates or presets (36 total — 33 existing + 3 genuinely new, since Bain-marie/Fridge Tasks aren't counted as new); `hot_hold_temp` category correctly reused (13 `LegalLimitReference` rows total, unchanged from Cluster D); Bain-marie Tasks and Fridge Tasks each gain exactly one new item without creating duplicate presets, and don't duplicate that item on reseed; all 3 new presets have correct item counts; venue-type tagging covers exactly 12 of 12 for all three segments including Event/Mobile/Street Food. `flutter analyze` clean — no schema change this cluster, `app_database.g.dart` confirmed unchanged.
- Remaining cluster (F: Front of House/Bar & Beverage/Hotel-Specific/Management Oversight — the last one, which also picks up the deferred `PROJECT_BIBLE.md` "Task Library Source Content" rewrite) not yet started.

## Task library load: Cluster F (Sprint 030 follow-up) — SIXTH AND FINAL cluster, completes Build Order item 4
Segments 18-21: Front of House / Service, Bar & Beverage, Hotel-Specific, Management & Compliance Oversight. Same approach as Clusters A-E throughout. No new method, frequency, or role-tier vocabulary — every value used already existed (`note_photo`, part of the original 8-value method vocabulary since Sprint 023, gets its first real use here — "Weekly food safety walk-round"). **This completes the full HORECA_TASK_LIBRARY.md load — all 21 segments, ~150 tasks, now in the app.**
- 28 new `TaskTemplate`s. 8 genuinely new `TaskPreset`s (Coffee Machine, Cellar Cooler, Keg System, Post-Mix System, Front of House, Bar & Beverage, Hotel-Specific, Management & Compliance Oversight Tasks) plus two existing presets from Clusters A/E gaining more items: Bain-marie Tasks gains 3 (the first three-way merge across three different clusters — 2 from A/E plus 3 from this cluster, 5 total), Fridge Tasks gains 1 (7 total).
- Two `LegalLimitReference` categories reused (`hot_hold_temp` for hot buffet/breakfast buffet/banqueting hot-hold — the same ≥63°C [LAW] figure Cluster A established, just checked at different service moments; `fridge_temp` for cold buffet display). Two genuinely new: `cellar_temp` (11-13°C cask ale target, [BEST]) and `buffet_out_of_temp_time` (max 4 hours, [FSA] — the library's first pure-duration limit, using `unit: 'hours'` rather than a temperature unit, since the schema's `unit` column is free text). "Breakfast buffet temperatures" combines a hot AND cold threshold in one task (hot ≥63°C / cold ≤8°C) — left structurally unlimited (no single min/max pair fits both), both figures explained in `fixInstructions` instead, same treatment as Cluster E's "All refrigeration temps at open".
- **Three venue-tagging decisions confirmed before building, per the standing process rule**, plus one mechanically-derived exclusion applying for the first time:
  1. Front of House: confirmed **exclude** Event/Mobile/Street Food (doesn't typically have a "dining area" or "customer toilets" in the traditional sense). Dark/Ghost Kitchen is *also* excluded here, but that's not a new judgment call — it's the first real application of the already-agreed "Dark/Ghost Kitchen = QSR/Restaurant minus FOH and Bar" derivation from the original Sprint 030 plan. 10 of 12.
  2. Bar & Beverage: confirmed **exclude** Event/Mobile/Street Food (cellar/keg/optics infrastructure doesn't fit a typical mobile setup — the least ambiguous Event/Mobile call so far). QSR and Bakery are hard-excluded by the matrix's own explicit ✗; Dark/Ghost Kitchen excluded per the same established "minus Bar" derivation. Fast Casual and Gastropub still tagged via their union derivation. 8 of 12.
  3. Hotel-Specific: **not a judgment call** — the matrix marks every column except Hotel itself as an explicit ✗, the most restrictive row in the whole document, and all 4 derived venue types clearly don't reduce to Hotel either. 1 of 12 (Hotel only).
  4. Management & Compliance Oversight: confirmed **include** Event/Mobile/Street Food — compliance oversight (EHO readiness, staff training, food safety review) applies to any food business regardless of size or format, arguably more universal than several already-included operational segments. All 12.
- Equipment-mapping judgment calls, extending established rules: "Cellar / keg temperature" (column says the generic "Fridge", title clearly means the purpose-built type) → Cellar Cooler, the same "title's specific subject overrides a generic column tag" pattern first used for Serve-Over Fridge (Cluster A). "Beer line cleaning" and "Post-mix / soda gun cleaned" (blank columns, unambiguous titles) → Keg System and Post-Mix System, the established blank-column pattern.
- Verified with a temporary repository-level test (deleted after, not part of this commit, run with an explicit 150s per-test timeout given the now much larger dataset, ~5 GB disk space confirmed beforehand): exactly 151 current-version templates (123 from Clusters A-E/example + 28 new) with no duplicate titles; re-seeding doesn't duplicate templates or presets (44 total — 36 existing + 8 genuinely new); both new legal limit categories correct, `hot_hold_temp`/`fridge_temp` correctly reused (15 rows total); equipment mapping resolves Cellar Cooler/Keg System/Post-Mix System/Coffee Machine correctly; Bain-marie Tasks gains exactly 3 items (5 total) and Fridge Tasks gains exactly 1 (7 total) without creating duplicate presets or duplicating items on reseed; all 8 new presets have correct item counts; venue-type tagging covers exactly 10/12 for Front of House, 8/12 for Bar & Beverage, 1/12 (Hotel only) for Hotel-Specific, and 12/12 for Management Oversight. `flutter analyze` clean — no schema change this cluster, `app_database.g.dart` confirmed unchanged.
- **`PROJECT_BIBLE.md`'s stale "Task Library Source Content" section rewritten** (deferred since Cluster A specifically for this moment, once all 21 segments and the full vocabulary were known): now documents `HORECA_TASK_LIBRARY.md` as the source (superseding "Full check list.docx", explicitly retired), lists all 21 segments with their exact `TaskTemplate.segment` slugs, the 10-value method vocabulary, the 13-named-value-plus-`custom` frequency vocabulary (noting "Per order"/"Per menu change"/"Per visit" all fold into `Event-Based`), the unchanged 3-level priority scheme, the 63-type equipment library (Sprint 028), and the new venue-type tagging structure (Sprint 029/030). Also fixed one adjacent stale cross-reference in "Venue Setup and Equipment Configuration" that still cited "18 types as of Sprint 011".
- **This completes Build Order item 4.** Remaining Build Order items: 5 (Due/overdue + time-windows + exit behaviour) and 6 (Visual/UX pass), then resume branding + remaining parked items.

## Known UI bugs (logged ahead of the visual/UX pass, Build Order item 6)
Three real UI defects found via manual use, not previously logged anywhere in DECISIONS_LOG.md or FEATURE_AUDIT.md — recorded now, ahead of building the visual/UX pass's Sub-sprint 1 (theme + shared widgets foundation), for an honest paper trail before they're fixed in Sub-sprint 2.
- **Manager/Top action bars are icon-only, no visible labels**: `manager_screen.dart` and `top_screen.dart` each have an identical 9-item `AppBar.actions` row of `IconButton`s (Venue Setup, Venue Details, Assign Tasks, Task Presets, Staff Management, Notification Rules, Maintenance Contacts, Back Up Now, Log out) — each carries only a `tooltip`, which doesn't surface on touch devices at all. A user unfamiliar with the icon glyphs has no way to tell what a button does before pressing it.
- **Staff Management: staff names wrap one letter per line**: `staff_management_screen.dart`'s `_buildStaffTile` uses a `ListTile` whose `trailing` is a `Row` of 3 full-text `TextButton`s (Change Tier / Reset PIN / Deactivate) at their natural, unconstrained width, with no `Expanded`/`Flexible` anywhere in the tile. `ListTile` gives `trailing` first claim on available width, leaving `title: Text(user.name)` almost none — the name wraps character-by-character into the remaining sliver of space. A real, visible defect, not a cosmetic nitpick.
- **Task Presets: no grouping between equipment-type and section presets**: `preset_management_screen.dart` renders all presets (now ~44, after the Sprint 030 task library load) as one flat, undifferentiated `ListView` in load order. The data already distinguishes equipment presets (`equipmentTypeId != null`) from segment presets (`segment != null`) via `_contextLabel`'s subtitle text, but there's no section header or grouping in the list itself — finding a specific preset means scrolling the whole flat list.
All three found during visual/UX pass planning (this session); to be fixed in Sub-sprint 2 of that pass, once the shared theme/widget foundation (Sub-sprint 1) exists to fix them properly rather than with another one-off patch.

## Visual/UX pass — Sub-sprint 1: theme + shared widget foundation (Sprint 031)
First of 5 planned sub-sprints (foundation → the 3 known bugs → staff screens → manager screens → director screens), per the plan proposed and agreed in full before building. Builds `lib/app/theme/` and `lib/core/widgets/` — both folders `ARCHITECTURE_LOCK.md`'s Folder Structure Rule already reserved but left empty since the project began. No screen is migrated to the new theme/widgets yet (that starts Sub-sprint 2); this sub-sprint is foundation only.
- **Visual direction was proposed and signed off via a published Artifact before any code was written** — a concrete palette (hex values), type scale, and applied mockups of the three logged bugs, built to let the user react to the actual look rather than a text description of hex codes. First time this project has used that flow. Signed-off direction: "clinical but warm" — a hygiene-teal accent (`#0E6E77`) used only where something is actionable, warm off-white/near-black neutrals instead of clinical blue-white or cold grey, and three state colours (pass green `#1E7A4C`, caution amber `#B5720A`, critical brick-red `#B23A2E`, each with a paired light background tint) kept strictly separate from the accent so colour never competes with itself.
- **Typeface changed from the signed-off artifact, per explicit correction during the build itself**: the artifact proposed IBM Plex Sans/Plex Mono via the `google_fonts` package. Before building, flagged a real tension against `PROJECT_BIBLE.md`'s offline-first principle (`google_fonts` fetches font files over the network on first use by default) and asked how to resolve it; the recommended "fetch at runtime, cache after" option was initially confirmed. Mid-build, explicit correction: use a font that "won't cause problems irrespective of the device being used" and "don't overcomplicate this" — so the typeface itself was dropped entirely. The **signed-off type scale (sizes/weights: 28/600 task title, 20/600 section header, 16/400 body, 16/600 button, 14/400 caption floor) is unchanged and fully implemented**; only the specific typeface changed, from a bundled/fetched custom font to Flutter's own default per-platform system font (Roboto/Segoe UI/San Francisco/etc. — whatever the OS already provides, zero dependency, zero network fetch, zero offline risk). Numeric readouts (temperatures, PINs, timestamps) get tabular-figure alignment via `AppTextTheme.numeric()` instead of a dedicated mono face — same digit-alignment effect the proposal wanted, without needing a second font.
- New `lib/app/theme/` (3 files): `app_colors.dart` (the named colour tokens, exactly matching the signed-off artifact's hex values), `app_text_theme.dart` (the type scale, system-font-based per above), `app_theme.dart` (the real `ThemeData` — component themes for `AppBar`/`Card`/`ElevatedButton`/`TextButton`/`IconButton`/`Chip`/`InputDecoration`, encoding `DESIGN_SYSTEM_LOCK.md`'s rules as code rather than leaving each screen to reproduce them: a 56dp-tall primary button per the Layout Rule's "strong primary button," a 48dp floor on icon/text buttons per the Touch Rule and Prohibited Drift's "no tiny icon-only critical actions," consistent card borders/radius per the Layout Rule's "consistent card structure"). Light-only — no dark mode exists anywhere in this app yet, so none was added here.
- New `lib/core/widgets/` (5 files), per `DESIGN_SYSTEM_LOCK.md`'s Component Consistency Rule ("buttons, cards, chips, inputs, alerts, and status badges must use shared widgets where possible") — none of these previously existed; every screen built its own inline before this: `PrimaryActionButton` (thin wrapper over the themed `ElevatedButton`), `StatusBadge` (pass/caution/critical — colour **+ icon + word**, never colour alone, directly implementing the Accessibility Rule and Colour Logic together), `AppCard` (standardised card padding), `IconActionButton` (icon + visible label underneath — the actual fix component for the logged "bare icon rows" bug, built here but not wired into any screen yet), `SectionHeader` (small uppercase section label — the fix component for the logged "Task Presets: no grouping" bug, also built but unwired).
- `app.dart`'s `MaterialApp.theme` changed from the one-line `ThemeData(primarySwatch: Colors.blue)` (the entire theme definition since the project began) to `AppTheme.light`. This alone visibly changes default component styling app-wide (app bar colour, card borders, button shape/size) even before any screen file is touched, which is itself the foundation's verification signal.
- No behaviour changes — strictly visual/structural, per the agreed scope boundary (Open Decision 7 from the visual/UX pass plan). Nothing in the three logged bugs is fixed yet; that's Sub-sprint 2, which will *use* these new components rather than patch the old ad hoc styling in place.
- Verified: `flutter analyze` clean on the first pass (no iteration needed — the Material/Flutter API surface used, e.g. `CardThemeData`, `WidgetStatePropertyAll`, `FontFeature.tabularFigures()`, matched this SDK version on the first attempt). A real Windows debug run against the existing dev database confirmed the app launches without error and the new theme's effect is visible app-wide (app bar, buttons, cards) without any screen-level code changes.
- Remaining sub-sprints: 2 (fix the 3 logged bugs using this foundation — `staff_management_screen.dart`, `manager_screen.dart`, `top_screen.dart`, `preset_management_screen.dart`), 3 (staff/base-tier screens), 4 (manager/mid-tier screens, likely needs its own further split given file count), 5 (top/director-tier screens).

## Open / Not yet decided
- All three task-taxonomy gaps (priority, method, frequency) logged here since Sprint 012 are now resolved — see "Task-taxonomy reconciliation (Sprint 023)" above. The old "Full check list.docx" 132-task load this pointed to is superseded and now fully retired — see "Target market + library research": HORECA_TASK_LIBRARY.md was the library source for the complete load (Build Order item 4, DONE — see the six "Task library load" entries above, Clusters A-F), feeding the venue-type tagging structure Sprint 029 built (`TaskTemplateVenueTypes` is now populated for all ~150 loaded tasks).
- Multi-site is only partially usable: creating a second `Site` is safe (Sprint 025), but `Area`/`EquipmentInstance`/`TaskSchedule`/`User` reads are not yet filtered by site, so two venues' data currently displays mixed together in shared lists. Needs its own sprint before real day-to-day multi-site use is viable.
- Follow-up: apply a preset to multiple staff at once (Sprint 026 built the per-staff mechanism so this is a natural loop, not a rework) — not yet scheduled.
- Follow-up: distinct dashboards for Supervisor vs. Venue Manager, and Regional vs. Executive — currently share identical UI per tier-pair (Sprint 027) — not yet scheduled.
- Follow-up: wire `EquipmentTypeVenueTypes`/`TaskPresetVenueTypes`/`TaskTemplateVenueTypes` into actual filtering UI (venue setup wizard's equipment offering, presets section, and the future task-library-driven assignment flow) once each has real tag data to filter with — Sprint 029 built the schema/repository layer only.
